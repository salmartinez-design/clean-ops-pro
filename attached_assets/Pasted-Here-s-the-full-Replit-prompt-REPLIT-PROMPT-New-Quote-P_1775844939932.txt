Here's the full Replit prompt:

REPLIT PROMPT — New Quote Page Redesign
Redesign the New Quote page (/quotes/new and the quote builder component) with the following changes. Do not touch any other pages. Verify all changes at the live production URL after deploying.

LAYOUT CHANGE — Two-Column Persistent Layout
Replace the current centered narrow card layout with a full-width two-column layout:

Left column (58%): Wizard steps (Customer Info → Property Details → Service & Pricing → Add-ons & Notes → Review). Keep the existing stepper nav at the top.
Right column (42%): Persistent sticky panel — always visible regardless of which step the user is on. Contains two sections stacked vertically:

Call Notes (top, ~55% of panel height) — textarea, auto-saves to quote draft every 10 seconds via background PATCH. Label: "Call Notes". Subtext below label: "Not visible to client." Highlight-to-push feature described below.
Price Preview (bottom, ~45% of panel height) — existing price preview component, unchanged.



Right column should be position: sticky; top: 24px so it stays visible on scroll.

HIGHLIGHT-TO-PUSH (Call Notes)
In the Call Notes textarea, when the user selects text (mouseup / touchend), detect if selection length > 3 characters. If so, show a small floating tooltip anchored near the selection with a single button: "Push to Job Notes".
On click: append the selected text to the office_notes field (the job notes field visible in Step 4 — Add-ons & Notes). If office_notes already has content, append with a line break. Dismiss the tooltip. Show a 1.5-second inline confirmation: "Added to job notes" in the Call Notes panel.
Use window.getSelection() for detection. Tooltip should be absolutely positioned, background #1A1917, text white, font-size 12px, border-radius 4px, padding 4px 10px.

SMART TECHNICIAN SUGGESTION — Phase 1 (Address)
In Step 1 (Customer Info), after the Zip Code field:

When a valid 5-digit zip is entered, call the existing zone lookup logic (same as booking widget zone check) to identify which zone that zip belongs to.
Then query the employees table for technicians (role = 'technician', status = 'active') who are assigned to that zone (via the employee_zones or service_zones join — use whatever the current zone assignment schema is).
Render a section below the zip field titled "Suggested Technicians" with a subtle #F7F6F3 background, 1px #E5E2DC border, border-radius 8px, padding 12px.
Show each matching tech as a compact row: avatar initial circle (brand color, 28px), full name, zone badge (zone color chip), and a distance label: "In zone" (since we don't have live GPS, just confirm zone match).
If no techs match the zone, show: "No techs assigned to this zone — job will be unassigned."
Store the matched tech list in component state (suggestedTechs) for use in Phase 2.


SMART TECHNICIAN SUGGESTION — Phase 2 (Date Selected)
In Step 3 (Service & Pricing or wherever the date picker lives), after a date is selected:

Take the suggestedTechs list from Phase 1.
Query the jobs table for jobs on the selected date where status NOT IN ('void', 'moved', 'skip') and technician is in the suggested tech list.
For each suggested tech, compute jobCount (number of confirmed jobs that day).
Re-render the tech suggestion section (now shown again in Step 3 below the date picker) with updated availability:

If jobCount = 0: green dot + "Available"
If jobCount = 1: yellow dot + "1 job that day"
If jobCount >= 2: orange dot + "2+ jobs that day"
If jobCount >= 4: show in muted gray, label "Likely unavailable"


Sort: available first, then by job count ascending.
Allow the user to click a tech row to pre-select them as the assigned technician. Selected state: border changes to brand color, name bolds.
The selected tech should carry forward to the job when "Save & Convert to Job" is clicked.


RETURNING CLIENT DETECTION
In Step 1, on the Phone field:

On blur (when user leaves the phone field), strip formatting and query the clients table for phone ILIKE '%{cleaned_phone}%'.
If a match is found, show an inline banner below the phone field:

Background #EBF4FF, border 1px solid #5B9BD5, border-radius 6px, padding 8px 12px.
Text: "Returning client — [Full Name]" with subtext: "Last service: [date] · [frequency]"
Two buttons: "Use this client" (auto-fills all Customer Info fields from the match) and "Not them" (dismisses banner, clears phone).


Do not auto-fill without the user clicking "Use this client."
Same logic on Email field blur.


NOTES AUTO-SAVE
Every 10 seconds, if the Call Notes textarea has changed since the last save, silently PATCH the quote draft to the DB (update quotes table call_notes column). Show a tiny "Saved" text (secondary text color, 11px) that fades in/out in the top-right corner of the Call Notes panel. No spinner, no toast — silent.
If no quote draft exists yet (user hasn't clicked Save Draft), create a new draft row automatically on the first auto-save with status = 'draft' and the current company_id / user_id.

MISC UI CLEANUP

The top stepper tab bar: increase contrast slightly — active step should use brand color fill (current tab background = brand color, white text). Inactive = #F7F6F3 background, #6B6860 text.
"Save & Convert to Job" button: keep as primary CTA (brand color). "Save Draft" should be a ghost button (border only, no fill).
Remove the outer card wrapper padding that's creating the narrow centered look. The form fields should use the full left column width with 24px horizontal padding.


VERIFICATION
After deploying, confirm at https://clean-ops-pro.replit.app/quotes/new (logged in as salmartinez@phes.io):

Two-column layout renders correctly — Call Notes panel is visible and sticky on the right at all times across all 5 steps.
Entering zip code 60453 shows technicians in the Oak Lawn zone.
Selecting a date in Step 3 shows availability dots next to each suggested tech.
Selecting text in Call Notes shows the "Push to Job Notes" tooltip.
Entering a phone number matching an existing client shows the returning client banner.
Price Preview is still visible in the right panel and updates as scope changes.

Do not modify any other pages, routes, or DB tables outside of the quotes table call_notes column addition (add if it doesn't exist via a startup migration: ALTER TABLE quotes ADD COLUMN IF NOT EXISTS call_notes TEXT).Sonnet 4.6Extended