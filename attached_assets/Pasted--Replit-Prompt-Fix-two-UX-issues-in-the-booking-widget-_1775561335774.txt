**Replit Prompt:**

```
Fix two UX issues in the booking widget at /book/phes-cleaning:

**Fix 1 — Price Summary timing and persistence**

After the user completes Step 2 (home details — sqft, bedrooms, bathrooms, and "how dirty is your home" question) and hits Continue, the base price must calculate immediately and populate the Price Summary tile at the START of Step 3 before any add-ons are selected.

- Price Summary tile must show the base service price as soon as Step 3 renders
- As the user selects add-ons, each add-on price appends to the summary and the total updates live
- If the user navigates backward to Step 2 or forward past Step 3, the Price Summary tile must remain static and retain all previously calculated values — base price plus any selected add-ons
- The only time the Price Summary tile should update is when the user explicitly selects or deselects an add-on on Step 3
- Do not reset or clear the Price Summary tile on any navigation event

**Fix 2 — Move "Special notes" field for ALL service types**

For all four service types — Deep Clean, Move In / Move Out, One-Time Standard Clean, and Recurring:
- Remove the Special notes field from Step 2 entirely for all scopes
- Add it at the bottom of the add-ons section on Step 3, after all add-on tiles have been displayed, for all scopes
- Label it the same: "Special notes or anything you'd like us to know"
- Placeholder text: "e.g. garage access code, fragile items in cabinets, areas to avoid..."
- This applies universally — no exceptions by scope

After making both changes, verify on the live production URL https://clean-ops-pro.replit.app/book/phes-cleaning by testing:
1. Deep Clean with 1,234 sqft — confirm base price shows at start of Step 3 before any add-on is selected
2. Select and deselect add-ons — confirm price summary updates correctly
3. Navigate back to Step 2 and forward again — confirm price summary retains all values and does not reset
4. Confirm Special notes field is gone from Step 2 for all four service types
5. Confirm Special notes field appears after all add-on tiles on Step 3 for all four service types
6. Test all four scopes — Deep Clean, Move In / Move Out, One-Time Standard Clean, Recurring — and confirm consistent behavior across all
```