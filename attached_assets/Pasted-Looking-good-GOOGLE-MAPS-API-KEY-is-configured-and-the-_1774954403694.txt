Looking good — `GOOGLE_MAPS_API_KEY` is configured and the Service Address field is showing "Enter your service address." Now let's make it behave exactly like MC's address verification.

**What MC does:**
- Types address → Google dropdown appears instantly
- Selects address → full address populates, zip auto-fills
- Shows the verified address in a clean summary card
- If address is outside service area → soft warning
- Address carries through entire booking flow automatically

---

**REPLIT PROMPT — GOOGLE MAPS ADDRESS AUTOCOMPLETE EXACTLY LIKE MC:**

Wire up the Service Address field in Step 1 of the booking widget at `/book/phes-cleaning` to use Google Maps Places Autocomplete. The `GOOGLE_MAPS_API_KEY` and `VITE_GOOGLE_MAPS_API_KEY` are already in Replit Secrets.

---

**Step 1 — Load Google Maps Places library:**

In the booking widget component, load the Google Maps script dynamically on mount if not already loaded:

```javascript
useEffect(() => {
  if (!window.google) {
    const script = document.createElement('script');
    script.src = `https://maps.googleapis.com/maps/api/js?key=${import.meta.env.VITE_GOOGLE_MAPS_API_KEY}&libraries=places`;
    script.async = true;
    script.defer = true;
    document.head.appendChild(script);
  }
}, []);
```

---

**Step 2 — Wire autocomplete to the Service Address input:**

```javascript
useEffect(() => {
  if (!window.google || !addressInputRef.current) return;

  const autocomplete = new window.google.maps.places.Autocomplete(
    addressInputRef.current,
    {
      componentRestrictions: { country: 'us' },
      fields: ['address_components', 'formatted_address', 'geometry'],
      types: ['address']
    }
  );

  autocomplete.addListener('place_changed', () => {
    const place = autocomplete.getPlace();
    if (!place.address_components) return;

    const get = (type) =>
      place.address_components.find(c => c.types.includes(type))?.long_name || '';

    const shortGet = (type) =>
      place.address_components.find(c => c.types.includes(type))?.short_name || '';

    const addressData = {
      formatted: place.formatted_address,
      street: `${get('street_number')} ${get('route')}`.trim(),
      city: get('locality'),
      state: shortGet('administrative_area_level_1'),
      zip: get('postal_code'),
      lat: place.geometry.location.lat(),
      lng: place.geometry.location.lng(),
      verified: true
    };

    setServiceAddress(addressData.formatted);
    setAddressComponents(addressData);
    setZipCode(addressData.zip);
    setAddressVerified(true);

    // Check service zone
    checkServiceZone(addressData.zip);
  });
}, [window.google]);
```

---

**Step 3 — Address field UI:**

Replace the current plain text input with this:

```jsx
<div style={{ position: 'relative' }}>
  <input
    ref={addressInputRef}
    type="text"
    value={serviceAddress}
    onChange={(e) => {
      setServiceAddress(e.target.value);
      setAddressVerified(false);
      setZoneStatus(null);
    }}
    placeholder="Start typing your address..."
    style={{
      width: '100%',
      padding: '12px 40px 12px 16px',
      fontSize: '16px',
      border: `1px solid ${addressVerified ? '#2D6A4F' : '#E5E2DC'}`,
      borderRadius: '8px',
      fontFamily: 'Plus Jakarta Sans',
      color: '#1A1917',
      background: '#FFFFFF',
      outline: 'none',
      boxSizing: 'border-box'
    }}
  />
  {/* Green checkmark when verified */}
  {addressVerified && (
    <span style={{
      position: 'absolute',
      right: '12px',
      top: '50%',
      transform: 'translateY(-50%)',
      color: '#2D6A4F',
      fontSize: '16px'
    }}>✓</span>
  )}
</div>

{/* Service zone status */}
{zoneStatus === 'in_zone' && (
  <p style={{ color: '#2D6A4F', fontSize: '13px', marginTop: '6px', fontFamily: 'Plus Jakarta Sans' }}>
    We service this area.
  </p>
)}
{zoneStatus === 'out_of_zone' && (
  <p style={{ color: '#6B6860', fontSize: '13px', marginTop: '6px', fontFamily: 'Plus Jakarta Sans' }}>
    We don't currently service this area. Call (773) 706-6000 to confirm availability.
  </p>
)}
```

---

**Step 4 — Service zone check function:**

```javascript
const checkServiceZone = async (zip) => {
  try {
    const response = await fetch(
      `/api/public/service-zones/check?zip=${zip}&companySlug=phes-cleaning`
    );
    const data = await response.json();
    setZoneStatus(data.inZone ? 'in_zone' : 'out_of_zone');
  } catch {
    setZoneStatus(null);
  }
};
```

Create the endpoint `GET /api/public/service-zones/check?zip=&companySlug=` — queries the service_zones table, returns `{ inZone: boolean, zoneName: string | null }`. No auth required.

---

**Step 5 — Auto-fill zip code field:**

When address is selected from autocomplete, automatically populate the Zip Code field with the zip extracted from the address. The customer should never need to manually enter zip after selecting from dropdown.

---

**Step 6 — Address state carries through all steps:**

Store the full `addressComponents` object in the booking form's top-level state. Pass it through to the final submission payload. On job creation, store:

```javascript
{
  service_address: addressData.formatted,
  address_street: addressData.street,
  address_city: addressData.city,
  address_state: addressData.state,
  address_zip: addressData.zip,
  address_lat: addressData.lat,
  address_lng: addressData.lng,
  address_verified: addressData.verified
}
```

---

**Step 7 — Remove all duplicate address fields:**

Search entire booking widget for any address input field that appears after Step 1. Remove all instances. "Where are we cleaning?" section — delete entirely. Address is entered once in Step 1 only.

---

**Step 8 — Remove discount code field permanently:**

Search entire widget for discount code input, promo code input, and Apply button. Delete all instances from every step.

---

**Step 9 — Google Places dropdown styling:**

Add this CSS to override the default Google autocomplete dropdown to match Qleno design system:

```css
.pac-container {
  font-family: 'Plus Jakarta Sans', sans-serif;
  border: 1px solid #E5E2DC;
  border-radius: 8px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.08);
  margin-top: 4px;
}

.pac-item {
  padding: 10px 16px;
  font-size: 14px;
  color: #1A1917;
  cursor: pointer;
}

.pac-item:hover {
  background: #F7F6F3;
}

.pac-item-selected {
  background: #F7F6F3;
}

.pac-matched {
  color: var(--brand);
  font-weight: 600;
}

.pac-icon {
  display: none;
}
```

---

Republish after changes. Test by typing "3165 W 102" in the Service Address field — confirm:
1. Google Places dropdown appears with matching addresses
2. Selecting an address populates the full address and auto-fills zip code
3. Green checkmark appears on the field after selection
4. "We service this area" or zone warning appears below
5. No duplicate address field on any subsequent step
6. No discount code field anywhere