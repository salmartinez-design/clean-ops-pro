Good catch. The root cause the agent just found tells us exactly what happened — the DB scope name is still **"Deep Clean or Move In/Out"** as one combined entry. It was never actually split into two separate records.

The agent fixed the widget display by using `includes("deep clean")` as a workaround, but the underlying data is still one scope. That means:

- Both cards are pulling from the same DB record
- Move In/Out is not truly its own scope yet
- The acknowledgment checkboxes for Move In/Out may not be triggering correctly
- Jobs created from either card are tagged identically in the DB

---

**REPLIT PROMPT — SCOPE SPLIT FIX:**

The scope "Deep Clean or Move In/Out" exists as a single record in the `pricing_scopes` table. It needs to be split into two separate records. Do the following:

**Step 1 — Database:**

```sql
-- Rename existing scope
UPDATE pricing_scopes 
SET name = 'Deep Clean'
WHERE name = 'Deep Clean or Move In/Out'
AND company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning');

-- Create new Move In/Out scope inheriting same pricing
INSERT INTO pricing_scopes (
  company_id, name, base_hourly_rate, billing_type, 
  pricing_method, technician_efficiency, pay_type,
  available_online, available_office
)
SELECT 
  company_id, 'Move In / Move Out', base_hourly_rate, billing_type,
  pricing_method, technician_efficiency, pay_type,
  true, true
FROM pricing_scopes
WHERE name = 'Deep Clean'
AND company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning');

-- Copy all pricing tiers from Deep Clean to Move In/Out
INSERT INTO pricing_tiers (scope_id, sqft_min, sqft_max, base_price)
SELECT 
  (SELECT id FROM pricing_scopes WHERE name = 'Move In / Move Out' 
   AND company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning')),
  sqft_min, sqft_max, base_price
FROM pricing_tiers
WHERE scope_id = (SELECT id FROM pricing_scopes WHERE name = 'Deep Clean'
  AND company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning'));
```

**Step 2 — Widget scope name checks:**

Update the three scope detection constants to match the new exact DB names:

```javascript
const isDeepCleanScope = selectedScope?.name?.toLowerCase().trim() === 'deep clean';
const isMoveInOutScope = selectedScope?.name?.toLowerCase().trim() === 'move in / move out';
const isRecurringScope = selectedScope?.name?.toLowerCase().trim() === 'recurring cleaning';
```

No more `includes()` workarounds — exact matches now that DB names are clean.

**Step 3 — Confirm behavior after fix:**

- Selecting "Deep Clean" → shows home details → cleanliness question → upsell card
- Selecting "Move In / Move Out" → shows acknowledgment checkboxes → home details → no upsell card — no cleanliness question
- Both scopes calculate price from the same tier table
- Jobs from Deep Clean tagged `scope: 'deep_clean'`
- Jobs from Move In/Out tagged `scope: 'move_in_out'`, `property_vacant: true`
- Dispatch board shows "VACANT" badge on Move In/Out jobs only

**Step 4 — Verify pricing parity:**

After migration, run a quick check: quote a 2,000 sqft Deep Clean and a 2,000 sqft Move In/Out — both should return identical prices.

Republish after all steps complete.

---

After this runs screenshot both scope cards selected separately — Deep Clean showing upsell card, Move In/Out showing the three acknowledgment checkboxes. That confirms the split is clean.