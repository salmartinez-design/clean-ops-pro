No — the scope fix prompt I just sent doesn't include it. That prompt only fixes the scope display. The last-cleaned question needs its own addendum. Here are my recommended options:

**The 4 options (my recommendation):**

- "Within the last 2 weeks"
- "2–4 weeks ago"
- "1–3 months ago"
- "Over 3 months ago — or never professionally cleaned"

**Why these four:**
- Granular enough to be actionable, broad enough not to cause friction
- Clear 30-day line sits between option 2 and 3
- Option 4 captures both lapsed and first-timers — same treatment either way

**Logic:**
- Options 1 or 2 → proceed with Recurring booking normally
- Options 3 or 4 → block recurring, show Deep Clean recommendation modal

---

**REPLIT PROMPT ADDENDUM — LAST CLEANED QUESTION:**

Add to the booking widget at `/book/phes-cleaning`. This applies only when "Recurring Cleaning" is selected in Step 2.

---

**After the scope cards in Step 2, when Recurring Cleaning is selected, show the following question immediately below the scope selection before square footage:**

Question label: "When was your home last professionally cleaned?"
Help text in secondary color (`#6B6860`, 13px): "This helps us send the right team prepared for your home."

Render as 4 selectable pill/card options in a 2x2 grid:

1. "Within the last 2 weeks"
2. "2–4 weeks ago"
3. "1–3 months ago"
4. "Over 3 months ago — or never"

Default: none selected. Selection is required to proceed.

---

**CONDITIONAL LOGIC — options 3 and 4 (over 30 days):**

If the customer selects option 3 ("1–3 months ago") or option 4 ("Over 3 months ago — or never"), do not allow them to proceed with Recurring Cleaning. Instead:

Show an inline recommendation block directly below the question — do not navigate away, do not show an error. Style as a warm informational card (`#FFFFFF` background, `var(--brand)` left border 3px, `#E5E2DC` border, 16px padding):

**Heading:** "We recommend starting with a Deep Clean"
**Body:** "When a home hasn't been professionally cleaned in over 30 days, a Deep Clean ensures the best results — and sets the foundation for a great recurring service. Most customers who start with a Deep Clean stay recurring long-term."
**Two buttons side by side:**
- Primary: "Book a Deep Clean Instead" — `var(--brand)` background, white text — on click: switches selected scope to Deep Clean, dismisses this card, continues flow
- Secondary: "Continue with Recurring Anyway" — white background, `var(--brand)` border and text — on click: allows them to proceed with Recurring but tags the job internally with `last_cleaned_flag: 'overdue'` so office can follow up

---

**CONDITIONAL LOGIC — options 1 and 2 (within 30 days):**

Proceed normally into the Recurring flow. No recommendation block shown. Store the selected option on the job record as `last_cleaned_response`.

---

**DATABASE:**

Add two columns to the `jobs` table if not already present:
- `last_cleaned_response` VARCHAR — stores the raw option selected ("within_2_weeks", "2_4_weeks", "1_3_months", "over_3_months")
- `last_cleaned_flag` VARCHAR — stores "ok" for options 1–2, "overdue" for options 3–4, null for non-recurring scopes

---

**DESIGN:**
- Pill/card options: `#FFFFFF` background, `#E5E2DC` border unselected, `var(--brand)` border + light brand tint when selected
- Recommendation card: warm, never alarming — this is a suggestion not a blocker for users who push through
- Plus Jakarta Sans throughout, no emojis, no dark mode

Republish after changes.

---

Once this runs, add it to the scope fix prompt and send both together — one republish, not two.