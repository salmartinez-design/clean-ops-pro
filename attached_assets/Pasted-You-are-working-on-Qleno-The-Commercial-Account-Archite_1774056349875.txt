You are working on Qleno. The Commercial Account Architecture 
sprint is partially complete. Schema, backend (15 endpoints), 
and the Accounts list/detail pages are all built and working.

This prompt completes the remaining 4 frontend pieces:

1. Job wizard commercial path
2. Dispatch board commercial job cards  
3. My Jobs mobile commercial view
4. Dashboard Needs Attention commercial alerts

Do NOT rebuild anything already working.
Read existing files before editing:
  - Job creation wizard component
  - Dispatch board component
  - My Jobs mobile component
  - Dashboard component

DESIGN SYSTEM — NON-NEGOTIABLE:
- Background: #F7F6F3
- Cards: #FFFFFF
- Primary text: #1A1917
- Secondary text: #6B6860
- Borders: #E5E2DC
- Accent: var(--brand)
- NO emojis, NO dark mode, light theme only
- Plus Jakarta Sans only

═══════════════════════════════════════════════════════
PIECE 1 — JOB WIZARD COMMERCIAL PATH
═══════════════════════════════════════════════════════

Find the existing job creation wizard/modal.
Add a client type selector as the first step 
before any existing fields.

STEP 0 — CLIENT TYPE (new, insert before Step 1):

Two option cards side by side:
  
  Left card:
    Label: "Residential Client"
    Sublabel: "Individual home, flat-rate billing"
    Icon: Home (lucide-react)
  
  Right card:
    Label: "Commercial Account"  
    Sublabel: "Property management, hourly billing"
    Icon: Building2 (lucide-react)

Selected card gets var(--brand) border.
Default: Residential (existing flow unchanged).

If Residential selected → existing wizard 
flow runs exactly as before. No changes.

If Commercial selected → replace client 
search field with this two-step selector:

STEP 1A — ACCOUNT SELECTION:
  Label: "Account"
  Searchable dropdown
  Query GET /api/accounts?active=true
  Each option shows:
    Account name (bold)
    Property count + payment method badge
    e.g. "Pinnacle Property Mgmt · 
          5 properties · Card on file"
  
  "Create new account" link at bottom 
  of dropdown → navigates to /accounts 
  with new account slide-over open

STEP 1B — PROPERTY SELECTION:
  Appears after account is selected.
  Label: "Property"
  Searchable dropdown
  Query GET /api/accounts/:id/properties?active=true
  Each option shows:
    Property name
    Full address
    Property type badge
    Unit count if apartment_building
  
  If property has access_notes set:
    Show amber info strip below selector:
    "Access: [access_notes]"
  
  "Add property to this account" link 
  → opens account detail properties tab 
  in new tab

STEP 2 — SERVICE TYPE + RATE:
  Label: "Service Type"
  Dropdown shows account's configured 
  service types first (from rate cards),
  then allows free text entry.
  
  On service type selection:
    Call GET /api/accounts/:accountId/rates/lookup
    ?service_type=[selected]
    
    If rate found:
      Show green confirmation strip:
      "Rate: $[amount]/[unit] — [service_type]
       from account rate card"
      billing_method auto-set (hourly/flat)
      hourly_rate field hidden (auto-filled)
      
      "Override for this job" toggle:
        Off by default
        When toggled on: reveals editable 
        hourly_rate field
        Logs override: note visible on job detail
    
    If no rate found:
      Show amber warning strip:
      "No rate configured for '[service_type]' 
       on this account."
      Reveal manual rate entry fields:
        Billing method: Hourly / Flat Rate
        Rate amount ($)
      
      "Add to account rate card" link 
      (opens rate card tab in new tab)
  
  Estimated hours field (hourly jobs only):
    Label: "Estimated duration (hrs)"
    Placeholder: "2.5"
    Helper: "Used for scheduling only. 
    Invoice calculated from actual clock time."

STEP 3 — SCHEDULE & TEAM:
  Existing smart dispatch flow unchanged.
  Property address feeds proximity calculation.
  
  If property has access_notes:
    Show amber card in this step:
    "Building access: [notes]"
    So dispatcher sees it when assigning tech.

STEP 4 — CONFIRMATION SUMMARY:
  Show all fields in a clean summary card:
  
  Account: [name]
  Property: [address]
  Service: [type]
  Billing: $[rate]/hr · Hourly
    OR  $[amount] · Flat Rate
  Scheduled: [date] · [time window]
  Team: [tech names]
  Payment: Card on file — auto-charge 
           on completion
    OR  Invoice only — [payment terms]
  
  For hourly jobs, show this note:
  "Final invoice amount calculated from 
   actual clock in/out time on completion."

═══════════════════════════════════════════════════════
PIECE 2 — DISPATCH BOARD COMMERCIAL JOB CARDS
═══════════════════════════════════════════════════════

Find the existing dispatch board component.
Modify job card rendering for jobs where 
account_id IS NOT NULL (commercial jobs).

COMMERCIAL JOB CARD LAYOUT:
Replace client name with this structure:

  Line 1: Account name (13px, weight 600, 
           primary text)
  Line 2: Property address (11px, secondary 
           text, truncated)
  Line 3: Service type badge + billing rate
           Service type: small pill badge 
           in var(--brand-soft) color
           Rate: "· $60/hr" in secondary text

Keep all existing elements:
  - Frequency color left border
  - Status badge
  - Team assignment
  - Time window
  - Drag handle (if applicable)

HOURS VARIANCE COLUMN:
For hourly jobs (billing_method = 'hourly'):
  Show: estimated_hours vs billed_hours
  
  Format: "est. [X]h · actual [X]h"
  
  If job is in_progress or scheduled:
    Show estimated hours only:
    "est. [X]h"
    
  If job is complete:
    Show both with color coding:
    Green: billed_hours ≤ estimated_hours
    Amber: billed_hours > estimated_hours 
           by ≤ 0.5h
    Red: billed_hours > estimated_hours 
         by > 0.5h
    
    Example: "est. 2.5h · actual 2.75h" (amber)

  For flat_rate jobs: show job price as before.
  For residential jobs: no change.

ACCESS NOTES ON JOB DETAIL:
When dispatcher clicks a commercial job 
to open the detail panel/modal:
  If property.access_notes is set:
    Show at the top of the detail panel,
    before all other fields:
    Amber card with label "Building Access":
    "[access_notes text]"
    This ensures dispatcher can relay 
    access info to tech if needed.

═══════════════════════════════════════════════════════
PIECE 3 — MY JOBS MOBILE COMMERCIAL VIEW
═══════════════════════════════════════════════════════

Find the existing My Jobs mobile component.
Modify job card and detail view for commercial 
jobs (account_id IS NOT NULL).

JOB LIST CARD (commercial):
  Replace client name display with:
  
  Primary: Account name (bold)
  Secondary: Property address
  
  Keep: time window, status, service type badge
  
  If property has access_notes:
    Show small amber indicator on the card:
    A small dot or "Access info" label
    (full notes visible on job detail)

JOB DETAIL VIEW (commercial):
  At the very top, before address:
  
  Account name section:
    "[Account name]" (16px, weight 600)
    Property name if set, or address
  
  If access_notes set:
    FULL amber card — prominent:
    Label: "Building Access"
    Content: full access_notes text
    (no truncation on detail view)
    This is critical — techs need gate 
    codes and access instructions visible 
    without hunting for them.
  
  Service type badge visible on detail.
  
  Billing info — NOT shown to tech:
    Do not show hourly rate, billed hours,
    or billing amounts on tech's mobile view.
    Billing is office-only information.
  
  Clock in/out behavior: unchanged.
  GPS geofencing: uses property lat/lng.
  Photo gate: unchanged.
  
  After clock out on commercial job:
    Show: "Job recorded. Time logged."
    (not "invoice created" — tech doesn't 
    need to know about billing)

═══════════════════════════════════════════════════════
PIECE 4 — DASHBOARD NEEDS ATTENTION ALERTS
═══════════════════════════════════════════════════════

Find the existing Needs Attention widget 
on the dashboard.

Add these commercial alert types to the 
existing alert rendering logic.
These appear alongside existing alerts 
(overdue invoices, GPS violations, etc.)

ALERT TYPE 1 — CHARGE FAILED (red):
  Trigger: jobs where charge_failed_at 
  IS NOT NULL AND charge_succeeded_at IS NULL
  
  Display:
  Red dot indicator
  "[Account Name] — $[amount] charge failed
   on [date] · [property address]"
  
  Actions:
    "Retry" → calls POST /api/accounts/:id/charge
              with the job_id
    "View" → navigates to account detail 
             Jobs & Invoices tab
  
  Priority: highest — show before all 
  other alert types

ALERT TYPE 2 — NO CARD ON FILE (amber):
  Trigger: accounts where 
  payment_method = 'card_on_file' AND
  stripe_customer_id IS NULL AND
  square_customer_id IS NULL AND
  is_active = true
  
  Display:
  Amber dot indicator
  "[Account Name] — no card on file. 
   Account expects auto-charge."
  
  Actions:
    "Send payment link" → triggers 
    POST /api/accounts/:id/send-payment-link
    (stubs to "coming soon" toast if 
    payment link sprint not yet complete)
    "View account" → navigates to 
    account detail page

ALERT TYPE 3 — HOURS VARIANCE (amber, optional):
  Trigger: completed commercial jobs today 
  where billed_hours > estimated_hours + 0.5
  (significant overrun only)
  
  Display:
  Amber dot indicator
  "[Tech name] ran [X]h over at 
   [property address] · $[extra_cost] 
   additional billed"
  
  Action:
    "Review" → opens job detail
    where owner can adjust billed_hours 
    before charge (if charge hasn't fired yet)
    
  Note: This alert only surfaces BEFORE 
  charge_attempted_at is set. Once charged, 
  the amount is final and the alert clears.

ALERT RENDERING RULES:
  Charge failures: always show, never auto-dismiss
  No card on file: show once per account, 
  dismiss when card is added
  Hours variance: show until charge fires 
  or owner reviews
  
  Keep existing alert types working:
  Overdue invoices, GPS violations, 
  pending discipline records — all unchanged.
  
  Sort order in widget:
  1. Charge failures (red)
  2. Hours variance awaiting review (amber)
  3. No card on file (amber)
  4. Existing alert types (unchanged order)

═══════════════════════════════════════════════════════
CONFIRMATION CHECKLIST
═══════════════════════════════════════════════════════

After implementation confirm:

JOB WIZARD:
1. Client type selector appears as first step
2. Residential selected → existing flow 
   runs completely unchanged
3. Commercial selected → account dropdown 
   loads from GET /api/accounts
4. Property dropdown filters by selected account
5. Access notes display in amber strip 
   when property has them
6. Rate auto-populates from lookup endpoint 
   when service type selected
7. Rate override toggle works and is logged
8. Amber warning shows when no rate found
9. Confirmation summary shows correct 
   billing and payment method info
10. Hourly note shows on confirmation 
    for hourly billing jobs

DISPATCH BOARD:
11. Commercial job cards show account name, 
    property address, service type badge, 
    and billing rate
12. Residential job cards completely unchanged
13. Hours variance column shows est vs actual 
    for hourly commercial jobs
14. Color coding correct: green/amber/red 
    based on variance threshold
15. Access notes visible in job detail panel 
    for commercial jobs

MOBILE MY JOBS:
16. Commercial jobs show account name 
    as primary and property as secondary
17. Access notes show as prominent amber 
    card on job detail — full text, 
    no truncation
18. Billing information NOT visible to tech
19. Post-clock-out message is neutral 
    ("Job recorded") not billing-related
20. GPS geofencing still works using 
    property coordinates

DASHBOARD:
21. Charge failed alert appears for jobs 
    where charge_failed_at is set
22. No card on file alert appears for 
    COF accounts without stored card
23. Hours variance alert appears for 
    significant overruns before charge fires
24. All existing alert types still work
25. Alert sort order correct: failures first

REGRESSION:
26. All existing residential job flows 
    completely unaffected
27. All 15 account API endpoints still 
    respond correctly
28. Existing dummy data (10 employees, 
    10 clients, 15 jobs) still loads
29. Pinnacle Property Management seed 
    account still visible at /accounts