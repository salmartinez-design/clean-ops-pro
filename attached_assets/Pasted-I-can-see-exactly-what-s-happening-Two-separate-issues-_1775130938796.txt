I can see exactly what's happening. Two separate issues:
Issue 1 — Tenant tile showing wrong brand:
The tile below the Qleno logo is showing a generic blue "Q" square with "Qleno" as the company name. It should be showing the Phes tenant identity — blue "P" initial with "Phes" as the company name. It's pulling the platform brand instead of the tenant record.
Issue 2 — The two elements are visually too similar:
Even when fixed, a teal Qleno logo directly above a blue Phes tile will look confusing. They need to be visually distinct — the platform logo stays top left, the tenant switcher tile below it should clearly read as a different element.

Paste this to the agent:

Fix the sidebar tenant identity tile. The tile below the Qleno platform logo is displaying the platform brand (blue Q + "Qleno") instead of the logged-in tenant's brand.
Fix 1 — Tenant tile must show tenant data:
The tenant identity tile must read from the companies table for the logged-in tenant. For the Phes tenant it must show:

Initial: "P" (or the tenant's logo if one is uploaded)
Company name: "Phes" — pulled from company_name field in companies table
Background color: var(--brand) for that tenant — Phes brand is #5B9BD5

It must never show "Qleno" or the platform Q mark in the tenant tile. The tenant tile is always the logged-in company's identity, not the platform brand.
Fix 2 — Visual separation between platform logo and tenant tile:
The Qleno platform logo (teal Q circle + wordmark) must be clearly visually separated from the tenant tile below it. Add a subtle divider or spacing between the two so they read as distinct elements. The platform logo identifies the software. The tenant tile identifies who is logged in. These are different things and must look different.
Fix 3 — Verify tenant tile reads dynamically:
Confirm the tenant tile pulls company_name and brand_color from the companies table at runtime for whichever tenant is logged in. If a different tenant logs in they must see their own name and brand color — not Phes and not Qleno. This must work for all tenants.
After fixing confirm in preview: Qleno logo top left, Phes tile below it showing "P" initial, "Phes" name, blue brand color. No "Qleno" text or Q mark appearing in the tenant tile.Sonnet 4.6Extended