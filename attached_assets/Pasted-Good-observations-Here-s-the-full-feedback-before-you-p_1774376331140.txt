Good observations. Here's the full feedback before you post anything:

---

**Dashboard issues visible right now:**

1. **All KPIs showing dashes** — Monthly Revenue, Avg Bill, Active Clients, Quality Score, Clients at Risk, Week Revenue are all empty. The job_history data is in production but the dashboard queries aren't hitting it. This needs the same union fix as the Jobs page.

2. **Today's Status all zeros** — No jobs scheduled because the recurring job engine still isn't built. This will stay zero until that sprint runs.

3. **"Good afternoon, there."** — The greeting is missing the name. Should be "Good afternoon, Sal." Pulling from the logged-in user record.

4. **Revenue this week showing dash** — Same root cause as the KPI issue.

5. **Needs Attention section empty** — Once jobs exist this will populate, but the logic needs to pull from job_history for overdue invoices and at-risk clients.

---

**Keyboard shortcut + help icon improvements:**

```
Make the following global UI improvements:

1. KEYBOARD SHORTCUT — Q for New Quote
   Add 'Q' as a global keyboard shortcut to open 
   the New Quote drawer/modal from anywhere in the app.
   
   Implementation:
   - Add a global keydown listener in the root App 
     component (or layout wrapper)
   - When key === 'Q' or key === 'q' AND the focused 
     element is NOT an input, textarea, or contenteditable
     → trigger the same action as clicking "+ New Quote"
   - Show a keyboard shortcut hint badge on the 
     New Quote button: small grey pill reading "Q"
     same style as the existing search shortcut 
     that shows "ctrl + K"
   - Add to the existing keyboard shortcuts list 
     if one exists, or create a shortcuts tooltip

2. ADDITIONAL SHORTCUTS — add these at the same time:
   J → New Job (same guard: not in input)
   I → New Invoice
   C → New Customer
   / → Focus the search bar (same as existing ctrl+K 
       if that exists, make both work)
   
   Show shortcut hints on each corresponding button
   as small grey pills matching the search bar style.

3. HELP ICON — replace ? with a better symbol
   The current ? icon in the top nav should be 
   replaced with a CircleHelp icon from lucide-react.
   Same size, same position, no other changes.
   This is cleaner and more intentional than a 
   raw question mark.

4. DASHBOARD — fix greeting to include user name
   "Good afternoon, there." should read 
   "Good afternoon, [first name]."
   Pull first name from the authenticated user's 
   profile. If name is not set, fall back to 
   "Good afternoon." (no "there").

5. DASHBOARD — fix KPI cards to pull from job_history
   The 6 KPI cards (Monthly Revenue, Avg Bill, 
   Active Clients, Quality Score, Clients at Risk, 
   Week Revenue) are showing dashes.
   
   Update the dashboard API endpoint to query 
   job_history in addition to or instead of jobs:
   
   Monthly Revenue:
     SUM(revenue) FROM job_history 
     WHERE job_date >= date_trunc('month', now())
     AND company_id = :companyId
   
   Week Revenue:
     SUM(revenue) FROM job_history
     WHERE job_date >= date_trunc('week', now())
     AND company_id = :companyId
   
   Avg Bill (last 30 days):
     AVG(revenue) FROM job_history
     WHERE job_date >= now() - interval '30 days'
     AND company_id = :companyId
   
   Active Clients:
     COUNT(*) FROM clients 
     WHERE is_active = true
     AND company_id = :companyId
   
   Clients at Risk:
     COUNT(*) FROM clients
     WHERE is_active = true
     AND last_service_date < now() - interval '45 days'
     AND company_id = :companyId
     (exclude clients migrated within last 30 days 
      to avoid false positives from migration)
   
   Quality Score:
     AVG(score) FROM scorecards
     WHERE created_at >= now() - interval '90 days'
     AND company_id = :companyId
   
   If any value is null or 0, show '—' not '$0' or '0'.
   Never show a dash if real data exists.

6. DASHBOARD — Revenue this week in header
   Top right "Revenue this week" should pull from 
   the same Week Revenue query above.
   Format as $X,XXX — no cents needed at this level.

Design: no changes to layout or colors.
Plus Jakarta Sans, no emojis, #F7F6F3 background.
Confirm clean build and publish.
```

---

The recurring job engine remains the blocker for Today's Status. Everything else in this prompt is fixable right now without it.