Honest take — the bones are solid. The dispatch board is actually clean. But you’re right, several screens need mobile work. Let me call out exactly what I see:

What’s Broken on Mobile
Employee Profile (Images 1 & 2)
The snapshot card — Hire Date, Skills, Tags, Score, Productivity — is rendering as a floating dropdown that overlaps the content instead of sitting inline. On desktop it’s probably a sidebar card. On mobile it needs to stack vertically below the photo/name header. This is the biggest visual bug.
Attendance Calendar (Image 5)
Getting cut off on the right. The stats panel is bleeding off screen. Same root cause as the dispatch board cutoff — the calendar isn’t constrained to viewport width.
Bottom Nav (All screens)
“My Jobs” is still showing for you as owner. That bug wasn’t fully fixed yet.
The teal color is fixed on dispatch — PHES blue is back. Good.

What Actually Looks Good
	∙	Dispatch date strip with stat cards is genuinely clean
	∙	Attendance stats list (Image 3 & 4) is readable and well structured
	∙	The PTO/Sick cards are clear
	∙	Bottom nav pattern (Today, Schedule, My Jobs, Customers, More) is the right approach

One Targeted Prompt to Fix All of It

Context: CleanOps Pro — clean-ops-pro.replit.app
Login: owner@phescleaning.com / demo1234
Tech: React + Tailwind + Plus Jakarta Sans
Design: #F7F6F3 bg, #FFFFFF cards, #1A1917 text, 
var(--brand) accent, NO emojis, NO dark mode

Fix all mobile layout issues below. Test every fix at 375px width.

---

FIX 1 — EMPLOYEE PROFILE SNAPSHOT CARD

On mobile, the snapshot card (showing Hire Date, Skills, Tags, 
Score, Productivity) is rendering as a floating overlay/dropdown 
that covers the profile content instead of laying out properly.

Fix:
- On desktop (768px+): keep the current layout — snapshot card 
  as a sidebar or inline card next to the photo/name header
- On mobile (< 768px): the snapshot card must render as a full-width 
  stacked card BELOW the photo/name/role header, not floating
- The card should show two columns of data side by side:
  Left column: Hire Date, Skills, Tags, Score
  Right column: Productivity percentages by service type
- No dropdown, no tooltip, no overlay — just a clean white card 
  with #E5E2DC border, 16px padding, stacked below the header
- The employee tabs (Information, Tags & Skills, Attendance, etc.) 
  appear below this snapshot card

---

FIX 2 — ATTENDANCE CALENDAR CUTOFF

The attendance calendar on the employee Attendance tab is 
getting cut off on the right side on mobile.

Fix:
- The calendar must be max-width: 100%, overflow hidden
- All 7 day columns must fit within the screen width at 375px
- Reduce day column padding if needed — prioritize fitting all 7 days
- The calendar legend (Worked, PTO, Time Off, Unexcused) must 
  wrap to two rows on mobile if needed, not overflow
- The stats panel (Scheduled, Worked, Absent, etc.) renders 
  as a full-width list BELOW the calendar on mobile
  On desktop it can stay side by side
- Today highlight and color coding must still work correctly

---

FIX 3 — HIDE MY JOBS FROM OWNER IN BOTTOM NAV

The bottom navigation bar on mobile shows:
Today | Schedule | My Jobs | Customers | More

"My Jobs" must NOT appear for role = owner, admin, or office.
It should only appear for role = technician.

Fix:
- In the mobile bottom nav component, apply the same role check 
  as the sidebar
- For owner/admin/office: bottom nav shows 
  Today | Schedule | Customers | More
  (remove My Jobs, shift remaining items)
- For technician: bottom nav shows 
  Today | My Jobs | Customers | More
- The "More" tab opens the slide-up sheet with secondary nav items

---

FIX 4 — GLOBAL MOBILE LAYOUT AUDIT

While fixing the above, do a pass on these pages at 375px 
and fix any overflow or cutoff issues:

/customers — customer list must not overflow horizontally
/invoices — invoice table must be readable, 
  consider hiding less important columns on mobile 
  (keep: client name, amount, status — hide: due date, created date)
/employees — employee list must fit at 375px
/quotes — quotes list same treatment as invoices

For all list/table pages on mobile:
- Show only the 2-3 most important columns
- Amount and status always visible
- Name always visible  
- Hide secondary columns (dates, IDs, etc.)
- Each row should be tappable and navigate to the detail page

---

FIX 5 — DISPATCH BOARD NEW JOB BUTTON

The + New Job button on the mobile dispatch board is not 
responding when tapped. 

Fix:
- Find and fix the onClick handler
- The button must open a bottom sheet (slides up from bottom)
- Bottom sheet contains a New Job form with:
  Client: typeahead search of existing clients
  Service: dropdown from quote_scopes (office-available only)
  Date: date picker defaulting to currently selected date
  Start time: time picker
  End time: time picker  
  Assigned employee: dropdown of active employees
  Amount: number input ($)
  Notes: optional textarea
  Save button: brand color, full width — saves job as 'scheduled',
  refreshes dispatch board without full page reload
- Bottom sheet has drag handle at top, X button, 
  dark backdrop behind it

---

TESTING CHECKLIST:
[ ] Employee profile snapshot card no longer floats/overlaps on mobile
[ ] Productivity percentages visible without overflow  
[ ] Attendance calendar fits at 375px, all 7 days visible
[ ] Attendance stats stack below calendar on mobile
[ ] My Jobs NOT in bottom nav for owner role
[ ] Bottom nav items reflow correctly without My Jobs
[ ] Customers list no horizontal overflow at 375px
[ ] Invoices table shows name/amount/status only on mobile
[ ] Employees list fits at 375px
[ ] New Job button opens bottom sheet on tap
[ ] New Job form saves and refreshes dispatch board
[ ] All fixes work at 375px AND 768px AND 1280px

After all fixes verified:
bash push-to-github.sh 'mobile layout fixes: profile card, 
calendar, bottom nav, dispatch new job, table responsive'


One bigger picture note — once these mobile fixes are done, your platform will be genuinely usable on a phone. That is your single biggest competitive advantage over MaidCentral. The moment a cleaning business owner can run their entire day from their iPhone without pinching and zooming, you have won the demo. Every fix you make to mobile right now directly converts to paying tenants later.
Want to run this now?​​​​​​​​​​​​​​​​