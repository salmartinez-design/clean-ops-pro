```
Fix the following two UI issues. Confirm clean build and publish.

=== FIX 1 — LOGO LETTER SPACING ===

In QlenoLogo.jsx and anywhere the wordmark renders
(sidebar, login page, favicon area, email templates):

Reduce the gap between the Q circle icon and the 
"leno" text by approximately 50%. The icon and 
wordmark should read as one unified element, not 
two separate pieces. Do not change size, color, 
font weight, or any other property — letter spacing 
and icon-to-text gap only.

=== FIX 2 — KEYBOARD SHORTCUTS REQUIRE SHIFT + KEY ===

The current single-key shortcuts (Q, J, I, C, /) 
must require Shift held down to trigger. This 
prevents accidental activation while typing in 
any text field, note, search box, or form.

New shortcut combinations:
  Shift + Q → New Quote
  Shift + J → New Job
  Shift + I → New Invoice
  Shift + C → New Customer
  Shift + / → Focus search bar

Update the global keydown listener in the root 
App component or layout wrapper:

  REMOVE this pattern:
    if (e.key === 'q' && !isInputFocused)

  REPLACE with:
    if (e.key === 'Q' && e.shiftKey && !isInputFocused)

  Apply same pattern to all other shortcuts.
  When Shift is held, e.key returns uppercase 
  automatically — check uppercase letters only.

The isInputFocused guard must still apply on all 
shortcuts. Do not trigger when focus is inside any:
  input, textarea, select, [contenteditable],
  .ProseMirror, or any element with role="textbox"

Update shortcut hint badges on all buttons:
  Old badge: grey pill showing "Q"
  New badge: grey pill showing "⇧Q"
  Use the ⇧ symbol (not the word "Shift") 
  to keep badges compact.

  Full badge updates:
    New Quote button    → "⇧Q"
    New Job button      → "⇧J"
    New Invoice button  → "⇧I"
    New Customer button → "⇧C"
    Search bar          → "⇧/"

If a keyboard shortcuts help tooltip or modal 
exists anywhere in the app, update it to show 
the new Shift combinations.

=== FIX 3 — DASHBOARD GREETING NAME ===

The greeting shows "Good afternoon." with no name.
It should read "Good afternoon, Sal."
Pull first_name from the authenticated user record 
in the production database.
If first_name is null or empty, fall back to 
"Good afternoon." with no comma and no "there."

=== FIX 4 — DASHBOARD KPI CARDS ===

All Key Numbers cards (Monthly Revenue, Avg Bill, 
Active Clients, Quality Score, Clients at Risk, 
Week Revenue) are showing dashes in production.

The production database has job_history data.
Update the dashboard API endpoint to query 
production correctly:

Monthly Revenue:
  SELECT SUM(revenue) FROM job_history
  WHERE DATE_TRUNC('month', job_date) = DATE_TRUNC('month', NOW())
  AND company_id = :companyId

Week Revenue:
  SELECT SUM(revenue) FROM job_history
  WHERE DATE_TRUNC('week', job_date) = DATE_TRUNC('week', NOW())
  AND company_id = :companyId

Avg Bill (last 30 days):
  SELECT AVG(revenue) FROM job_history
  WHERE job_date >= NOW() - INTERVAL '30 days'
  AND company_id = :companyId

Active Clients:
  SELECT COUNT(*) FROM clients
  WHERE is_active = true
  AND company_id = :companyId

Clients at Risk:
  SELECT COUNT(*) FROM clients
  WHERE is_active = true
  AND last_service_date < NOW() - INTERVAL '45 days'
  AND id NOT IN (
    SELECT DISTINCT client_id FROM clients
    WHERE created_at >= NOW() - INTERVAL '30 days'
  )
  AND company_id = :companyId

Quality Score:
  SELECT AVG(score) FROM scorecards
  WHERE created_at >= NOW() - INTERVAL '90 days'
  AND company_id = :companyId

Revenue this week header (top right of greeting card):
  Use the same Week Revenue query above.
  Format as $X,XXX — no cents.

Rules for display:
  If value is NULL or 0 and no data exists → show —
  If value is a real 0 (e.g. zero jobs today) → show $0
  Never show — when real data exists.
  Never show $0 for revenue when no jobs exist.

Confirm clean build and publish.
```