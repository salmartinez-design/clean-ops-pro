Before the prompt — here's the answer to your safeguard question:

**The root problem:** The agent keeps fixing dev and reporting "done" without verifying production. Every prompt needs to end with a live production URL test, not a DB query or dev preview.

**The safeguard going forward — add this to every prompt:**

> After all fixes are complete, verify each item by hitting the live production URL at `https://clean-ops-pro.replit.app` directly. Do not use the dev preview, do not query the DB and assume it worked. Open the actual live URL, perform the action, screenshot or log the result. If the live URL does not reflect the fix, the prompt is not done.

I'll add this to every prompt from now on.

---

**REPLIT PROMPT — SETTINGS CLEANUP + ZONE FIX + PRODUCTION VERIFICATION:**

Read all relevant files before making any changes. Every fix must be verified on the live production URL `https://clean-ops-pro.replit.app` — not dev preview, not DB queries alone.

---

**Fix 1 — Deep Clean and Move In/Move Out must be completely separate:**

In Pricing & Scopes, "Deep Clean or Move In/Out" is a combined legacy scope that must be hidden. The two correct separate scopes are "Deep Clean" (scope_id=1) and "Move In / Move Out" (scope_id=12). 

Do this:
- Find the combined "Deep Clean or Move In/Out" scope in the DB — set is_active = false AND add a flag to hide it from the UI entirely
- Confirm "Deep Clean" and "Move In / Move Out" appear as two completely separate rows in Pricing & Scopes
- Confirm the booking widget Step 2 shows "Deep Clean" and "Move In / Move Out" as two separate scope cards
- Both scopes must have their tiers, frequencies, and add-ons pre-filled from the existing DB data — do not reset any pricing data

**Production verify:** Log into `https://clean-ops-pro.replit.app/company?tab=pricing` and confirm two separate rows — no combined scope visible.

---

**Fix 2 — All Pricing & Scopes data pre-filled to match booking widget:**

Every scope in Company Settings must show its actual DB data pre-filled — not blank fields. Specifically:

- Deep Clean: $70/hr, Min $210, 20 tiers visible under Pricing Tiers tab
- Move In / Move Out: $70/hr, Min $210, 20 tiers visible
- One-Time Standard Clean: $65/hr, Min $150, 18 tiers visible
- Recurring Cleaning (grouped): Weekly $60/hr, Every 2 Weeks $65/hr, Every 4 Weeks $70/hr — each with their correct tiers
- All add-ons pre-filled with correct prices matching the booking widget

**Production verify:** Expand each scope in Company Settings and confirm tiers are showing, not blank.

---

**Fix 3 — Zone check failing for 60805 in production:**

Run these two queries directly on the PRODUCTION database and report which one returns results:

```sql
-- Query A
SELECT id, name, zip_codes, location FROM service_zones 
WHERE company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning')
AND '60805' = ANY(zip_codes::text[]);

-- Query B  
SELECT id, name, zip_codes, location FROM service_zones
WHERE company_id = (SELECT id FROM companies WHERE slug = 'phes-cleaning')
AND zip_codes::text ILIKE '%60805%';
```

Use whichever query returns results to fix the zone check endpoint. Update the endpoint to use the correct array matching syntax for production.

**Production verify:** Hit `https://clean-ops-pro.replit.app/api/public/zone-check?zip=60805` directly and confirm response shows `inZone: true, location: oak_lawn`. Then test 60173 → schaumburg, 99999 → false.

---

**Fix 4 — Schaumburg zones not showing in Service Zones tab:**

The Schaumburg tab shows "No zones for this location." Fix the filter query in ServiceZonesTab to correctly filter service_zones where location = 'schaumburg'.

**Production verify:** Log into Company Settings → Service Zones → Schaumburg tab. Confirm zones appear with correct zips including 60173, 60194, 60195, 60196.

---

**Fix 5 — Company Settings tab bar overflow:**

The settings tabs (HR Policies, Documents, Integrations) are hidden off-screen and require scrolling right to find. Fix the tab bar so all tabs are accessible without horizontal scrolling.

Solutions in order of preference:
1. Wrap tabs to a second row if they overflow
2. Use a scrollable tab bar with visible scroll indicators and left/right arrow buttons
3. Group related tabs into dropdown menus — Operations (General, Branding), Pricing (Pricing & Scopes, Add-ons, Online Booking, Service Zones), Communication (Follow-Up Sequences, Notifications), HR (HR Policies, Documents), System (Clock In/Out, Invoicing, Payroll Options, Integrations)

Use whichever approach fits cleanest with the existing design system. No emojis. No dark mode.

**Production verify:** Log into Company Settings on both desktop and a 1280px wide browser window. Confirm all tabs including HR Policies, Documents, and Integrations are visible and clickable without horizontal scrolling.

---

**Fix 6 — Recurring scope grouping:**

Scopes 4 (Weekly), 9 (Every 2 Weeks), 10 (Every 4 Weeks) must show as ONE "Recurring Cleaning" accordion with three tabs inside. Currently "Recurring Cleaning - Weekly" still appears as a standalone row.

**Production verify:** Log into Company Settings → Pricing & Scopes. Confirm only one "Recurring Cleaning" row exists, expanding to show Weekly / Every 2 Weeks / Every 4 Weeks tabs.

---

**CRITICAL RULE FOR THIS PROMPT:**

After each fix, test on the live production URL before moving to the next fix. Do not report a fix as complete based on dev preview or DB query alone. If the production URL does not reflect the change, the fix is not done. Report the production URL test result for each fix explicitly.

---

**Final verification table — all must pass on production URL:**

| Test | Production URL | Expected |
|---|---|---|
| V1 | /company?tab=pricing | No combined "Deep Clean or Move In/Out" row |
| V2 | /company?tab=pricing | Deep Clean and Move In/Move Out as separate rows |
| V3 | /company?tab=pricing | Tiers pre-filled under each scope |
| V4 | /api/public/zone-check?zip=60805 | inZone: true, oak_lawn |
| V5 | /api/public/zone-check?zip=60173 | inZone: true, schaumburg |
| V6 | /api/public/zone-check?zip=99999 | inZone: false |
| V7 | /company?tab=zones → Schaumburg tab | Schaumburg zones visible |
| V8 | /company → all tabs visible | No horizontal scroll needed |
| V9 | /company?tab=pricing → Recurring | One grouped accordion, three tabs inside |

Do not close until all 9 pass on the live production URL.