Got it. Clean bar. Here's the full prompt covering everything from those screenshots:

---

**Make these targeted changes to the client profile only. Do not touch any other page, component, or feature.**

**1. Hero action bar — replace buttons:**
Remove the current buttons and replace with exactly these four in this order:
`Schedule Job` | `Quote` | `Message` | `Edit`
- **Quote** button opens the existing quote builder pre-filled with this client
- Remove Invoice button entirely

**2. Revenue tile — update labels and colors:**
- Top line: Lifetime revenue amount — label below: `LIFETIME VALUE` (keep as-is)
- Bottom line: 2026 YTD revenue amount — label below: `2026 REVENUE` (not "2026 SO FAR" or "2026 SO FAR LEFT")
- Colors: use white text on dark slate (#1A1917) background for the tile — make both numbers white and clearly legible. The secondary number should be slightly smaller than the primary but still clearly readable.

**3. Service address on Property tab:**
- The address section currently shows only "+ Add Another Home" — this means the existing address is not rendering. Fix the query to pull all service addresses for this client and display each one as a card
- Each address card must show: street, city, state, zip — and a **zone color dot** matching the zone assigned to that address (use the same zone color system already in the app)
- The "+ Add Another Home" button stays below the existing address cards — it adds a new address alongside existing ones, never replacing them
- If a client has multiple addresses, all display as separate cards in the same section

**4. Zone color dot in hero strip:**
- Next to Jim's name in the hero strip, add a small colored dot matching his primary service address zone color
- Use the existing zone color mapping already in the codebase
- Tooltip on hover: zone name (e.g. "Tinley/Orlando/Palos Park")

**5. Fix Notes field on Property tab:**
- Currently displaying raw pipe-separated text: "Service: Hourly Standard Cleaning | Default Start: 08:00 AM | Address: 11535 Elbridge Ave... | Zone: Tinley/Orlando/Palos Park"
- This data is now stored in proper fields — clear this notes field entirely for Jim Schultz (set to empty string)
- The notes field should be a clean free-text area for manual office notes only

**6. Fix Acquisition Source for Jim Schultz:**
- Currently showing "Other" — update to "Recurring Client" in his client record

**Verify** at https://clean-ops-pro.replit.app/customers/23:
- Hero bar shows: Schedule Job | Quote | Message | Edit
- Revenue tile shows clean white text, "LIFETIME VALUE" and "2026 REVENUE" labels
- Property tab shows 11535 Elbridge Ave as an address card with zone color dot
- Zone color dot visible next to Jim's name in hero strip
- Notes field is empty
- Acquisition Source shows "Recurring Client"

Do not push to GitHub until confirmed.

---

Ready to paste?