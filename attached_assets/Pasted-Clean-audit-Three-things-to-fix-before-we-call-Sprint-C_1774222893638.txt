Clean audit. Three things to fix before we call Sprint C done. Here are the targeted prompts — paste them one at a time.

---

## Fix 1 — Quote builder infinite loop (critical)

```
Fix the React "Maximum update depth exceeded" crash in 
quote-builder.tsx.

When a scope is selected in the quote builder, a setState 
call is triggering inside a render cycle causing infinite 
re-renders and hitting the error boundary.

Find the useEffect or event handler in quote-builder.tsx 
that calls setState after scope selection. The likely cause 
is a missing or incorrect dependency array on a useEffect, 
or a state update being triggered directly in the render 
function rather than in a handler.

Fix the infinite loop so that:
1. Selecting a scope updates the scope state once
2. The price calculation fires once after scope + sqft 
   are both set
3. The live price panel renders without crashing
4. Changing sqft, frequency, or add-ons updates the 
   price panel without re-render loops

Do not change any other functionality in the quote builder.
```

---

## Fix 2 — Company name and right panel content

```
Fix two issues on the /book/phes-cleaning iFrame widget:

1. Company name shows as "Phes" instead of 
   "PHES Cleaning LLC". Find where the public company 
   endpoint GET /api/public/company/:slug returns the 
   company name and ensure it returns the full 
   company_name field, not a truncated or formatted 
   version.

2. The right panel should show phone number, email, 
   business hours, and office locations pulled from 
   the company record. Verify these fields are being 
   returned by the public company endpoint and rendered 
   in the right panel component. If any fields are 
   missing from the API response or not rendering in 
   the UI, fix them.

Do not change the booking flow, pricing, or any other 
widget functionality.
```

---

## Fix 3 — Portal button

```
Fix the "View Portal" button on the customer detail page.

Currently it is implemented as a JavaScript button with 
no href. It should navigate to /portal/phes-cleaning 
(using the authenticated company's slug dynamically).

Change it to either:
- An anchor tag with href="/portal/{companySlug}" where 
  companySlug comes from the authenticated company record
- Or a button with onClick that calls 
  window.open(`/portal/${companySlug}`, '_blank') or 
  navigate(`/portal/${companySlug}`)

The slug for PHES is "phes-cleaning". Ensure the slug 
field exists on the company record and is used here.

Do not change anything else on the customer detail page.
```

---

Run them in order. Fix 1 first since the quote builder crash is the only hard failure. Fixes 2 and 3 can run after. Paste results when done and we move straight to Sprint D.