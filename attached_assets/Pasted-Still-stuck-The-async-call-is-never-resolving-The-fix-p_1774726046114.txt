Still stuck. The async call is never resolving. The fix prompt didn't switch to synchronous calculation — it's still waiting on an API response that never comes back.

**REPLIT PROMPT — FORCE SYNCHRONOUS UPSELL PRICE:**

The upsell price calculation in the booking widget is permanently stuck on "Calculating..." because the async pricing API call never resolves. 

**Remove the async call entirely. Replace with direct synchronous calculation right now:**

Find the upsell card component. Find where `calculateUpsellRate` or any async price fetch is being called. Delete it completely. Replace with this synchronous function inline:

```javascript
const calculateUpsellPrice = (sqft, cadence, discountPercent = 15) => {
  if (!sqft || sqft <= 0) return null;

  // Recurring cleaning tiers — $65/hr base
  const tiers = [
    { min: 0,    max: 999,   price: 195 },
    { min: 1000, max: 1299,  price: 214.50 },
    { min: 1300, max: 1599,  price: 247 },
    { min: 1600, max: 1899,  price: 280 },
    { min: 1900, max: 2199,  price: 312 },
    { min: 2200, max: 2499,  price: 344 },
    { min: 2500, max: 2799,  price: 377 },
    { min: 2800, max: 3099,  price: 409 },
    { min: 3100, max: 3399,  price: 442 },
    { min: 3400, max: 3699,  price: 474 },
    { min: 3700, max: 3999,  price: 507 },
    { min: 4000, max: 4299,  price: 520 },
    { min: 4300, max: 4599,  price: 546 },
    { min: 4600, max: 4899,  price: 572 },
    { min: 4900, max: 5199,  price: 598 },
    { min: 5200, max: 5499,  price: 624 },
    { min: 5500, max: 5799,  price: 650 },
    { min: 5800, max: 6099,  price: 676 },
    { min: 6100, max: 99999, price: 702 },
  ];

  const cadenceMultipliers = {
    'weekly':       0.80,
    'biweekly':     0.90,
    'every_4_weeks': 1.00
  };

  const tier = tiers.find(t => sqft >= t.min && sqft <= t.max);
  if (!tier) return null;

  const multiplier = cadenceMultipliers[cadence] || 1.00;
  const recurringRate = Math.round(tier.price * multiplier * 100) / 100;
  const firstVisitRate = Math.round(recurringRate * (1 - discountPercent / 100) * 100) / 100;

  return { recurringRate, firstVisitRate };
};
```

Call this function synchronously on every cadence pill selection and on every sqft input change:

```javascript
const priceResult = calculateUpsellPrice(
  parseInt(squareFootage),
  selectedUpsellCadence,
  offerSettings?.upsell_discount_percent ?? 15
);
```

**Display logic:**
```javascript
if (!squareFootage || squareFootage <= 0) {
  // Show: "Enter your home size above to see your rate"
} else if (!selectedUpsellCadence) {
  // Show nothing in price area — waiting for cadence selection
} else if (priceResult) {
  // Show: 
  // "Your first recurring cleaning: ~~$[priceResult.recurringRate]~~ $[priceResult.firstVisitRate]"
  // "Then $[priceResult.recurringRate]/visit — locked for 24 months."
  // Button: "Yes — lock in my rate" — ACTIVE
} else {
  // Tier not found — show: "Contact us for a custom rate"
}
```

**No loading state. No spinner. No "Calculating..." text anywhere.**
The function runs in microseconds — there is nothing to wait for.

**Also fix the button:**
- Remove "Calculating..." from button text entirely
- Button shows "Yes — lock in my rate" always
- Button is disabled (grayed) until cadence is selected AND sqft > 0 AND priceResult is not null
- Button is active (full `var(--brand)` color) when all three conditions are met

**Remove all of the following from the upsell card component:**
- Any `useEffect` that fetches pricing
- Any `async` function call related to upsell pricing
- Any loading state variable (`isCalculating`, `calculating`, etc.)
- Any `setTimeout` or `debounce` on price calculation
- "Calculating..." text anywhere in the component

Republish after fix. Screenshot with 2000 sqft entered and "Every 2 Weeks" selected — confirm a real dollar amount shows instantly with no loading state.