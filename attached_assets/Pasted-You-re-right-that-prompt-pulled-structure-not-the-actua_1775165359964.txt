You're right — that prompt pulled structure, not the actual client records. Here's what you need:

---

**REPLIT AGENT PROMPT — Full Client Data Export for Review**

```
STOP. Read-only only. Do not change anything.

Run these queries and show me the full results:

-- 1. All PHES commercial clients (company_id = 1)
SELECT 
  id, first_name, last_name, company_name, email, phone,
  address, city, zip, client_type, payment_terms,
  frequency, service_type, base_fee, allowed_hours,
  notes, home_access_notes, is_active, account_id,
  property_group_id
FROM clients
WHERE company_id = 1 AND client_type = 'commercial'
ORDER BY company_name;

-- 2. All PHES residential clients with recurring schedules
SELECT 
  c.id, c.first_name, c.last_name, c.email, c.phone,
  c.address, c.city, c.zip, c.frequency, c.service_type,
  c.base_fee, c.allowed_hours, c.is_active,
  r.id as schedule_id, r.frequency as schedule_frequency,
  r.service_type as schedule_service_type,
  r.base_fee as schedule_fee, r.day_of_week,
  r.duration_minutes, r.notes as schedule_notes,
  r.assigned_employee_id, r.is_active as schedule_active
FROM clients c
LEFT JOIN recurring_schedules r ON r.customer_id = c.id
WHERE c.company_id = 1 AND c.client_type = 'residential'
AND r.id IS NOT NULL
ORDER BY c.last_name;

-- 3. PHES residential clients with NO recurring schedule
SELECT 
  id, first_name, last_name, email, phone,
  address, city, zip, frequency, service_type,
  base_fee, is_active, created_at
FROM clients
WHERE company_id = 1 
AND client_type = 'residential'
AND is_active = true
AND id NOT IN (
  SELECT DISTINCT customer_id 
  FROM recurring_schedules 
  WHERE company_id = 1
)
ORDER BY last_name;

-- 4. Recurring schedules with no matched client
SELECT 
  r.id, r.customer_id, r.frequency, r.service_type,
  r.base_fee, r.day_of_week, r.notes, r.is_active
FROM recurring_schedules r
WHERE r.company_id = 1
AND r.customer_id NOT IN (
  SELECT id FROM clients WHERE company_id = 1
);

-- 5. Count summary
SELECT
  (SELECT COUNT(*) FROM clients WHERE company_id = 1 AND client_type = 'residential' AND is_active = true) as active_residential,
  (SELECT COUNT(*) FROM clients WHERE company_id = 1 AND client_type = 'commercial') as commercial,
  (SELECT COUNT(*) FROM clients WHERE company_id = 1 AND is_active = false) as inactive,
  (SELECT COUNT(*) FROM recurring_schedules WHERE company_id = 1 AND is_active = true) as active_schedules,
  (SELECT COUNT(*) FROM recurring_schedules WHERE company_id = 1 AND assigned_employee_id IS NULL) as unassigned_schedules;

Show me every row. Do not truncate results.
```