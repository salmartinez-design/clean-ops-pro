Paste this into Replit:

---

**SPRINT: Recurring Job Engine — Generate Future Jobs from Imported Schedules**

We have 86 recurring schedules imported from MaidCentral in the `recurring_schedules` table. No future jobs are being generated from them. Build the full recurring job engine now.

**1. Understand the existing schema**

First read the `recurring_schedules` table structure and the `jobs` table structure. Identify all relevant fields — frequency, day of week, assigned technician, customer, branch, start date, time, duration, and any status fields.

**2. Job generation logic**

Build a function `generateJobsFromSchedule(schedule)` that:
- Reads the frequency field (weekly, bi-weekly, monthly)
- Generates the next 8 weeks of future jobs from today's date (March 24, 2026)
- Sets each job's status to `scheduled`
- Assigns the technician from the schedule's `assigned_technician_id` — if null, set to unassigned
- Sets customer, branch, service type, duration, and time from the schedule record
- Does not generate duplicate jobs — check if a job already exists for that customer + date + time before inserting

**3. Bulk generation on startup**

On server startup, run a check:
```
For each active recurring_schedule where company_id = 1:
  Count future jobs linked to this schedule
  If count < 4, generate jobs to fill the next 8 weeks
```
This ensures the dispatch board is never empty for active recurring clients.

**4. Ongoing generation — weekly cron**

Add a cron job that runs every Monday at 6am:
- For each active recurring schedule across all tenants
- Generate any missing jobs for the next 8 weeks
- Skip schedules where `status = inactive` or `paused`

**5. Link jobs back to schedule**

Each generated job must have a `recurring_schedule_id` field linking it back to its parent schedule. This enables:
- Skip and reschedule workflows
- Cancellation that pauses the schedule
- Edit one vs edit all future jobs

**6. Dispatch board validation**

After generation runs, confirm:
```sql
SELECT COUNT(*) FROM jobs 
WHERE company_id = 1 
AND status = 'scheduled' 
AND scheduled_date >= CURRENT_DATE;
```
Should return a meaningful number — at least 50+ jobs across the next 8 weeks given 86 recurring schedules.

**7. Handle the 9 schedules with no assigned technician**

For the 9 recurring schedules where `assigned_technician_id` is null, generate the jobs anyway with status `unassigned`. These should appear on the dispatch board in the unassigned column so they can be manually assigned before the job date.

**After completion confirm:**
- Future jobs visible on the dispatch board
- Jobs linked to correct customers and technicians
- Unassigned jobs appear in unassigned column
- No duplicate jobs generated on repeated startup

---

This is the most important prompt of the week. Everything else can wait until this comes back clean.