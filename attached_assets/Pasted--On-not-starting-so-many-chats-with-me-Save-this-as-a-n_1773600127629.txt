**On not starting so many chats with me:**

Save this as a note on your phone or desktop. Paste it at the start of every new conversation with me:

```
Context: I am Sal Martinez, owner of PHES Cleaning LLC 
in Oak Lawn IL. I am building a multi-tenant SaaS platform 
for cleaning businesses called CleanOps Pro.

Live app: https://clean-ops-pro.replit.app/login
GitHub: https://github.com/salmartinez-design/clean-ops-pro
Login: owner@phescleaning.com / demo1234
Super admin: sal@cleanopspro.com / SalCleanOps2026!

Tech stack: React + Express + Supabase + Cloudflare R2
Payments: Stripe Connect (tenants) + Square OAuth (PHES only)
Subscription: $100/month flat per tenant via Stripe Billing

WHAT'S BUILT:
- Multi-tenant auth, role hierarchy, RLS security
- Dashboard, Jobs, Employees, Customers, Invoices, Payroll
- Loyalty program, Discounts system
- Super admin portal at /admin
- Company branding (dynamic color + logo per tenant)
- Cleancyclopedia structure
- Light theme throughout (Plus Jakarta Sans font)
- GitHub connected, code pushed

WHAT'S NOT BUILT YET:
- Geo-fencing clock-in/out + My Jobs mobile view
- Before/after photo system with hard gate
- Employee profile deep build (scorecard, attendance)
- Native invoicing with line item editing
- QBO bidirectional sync
- Stripe billing subscription flow
- Online quoting iFrame
- Built-in eSign
- Client portal
- Form builder
- Payroll summary PDF export

DESIGN SPEC:
- Light theme: bg #F7F6F3, cards #FFFFFF, text #1A1917
- Dynamic brand color via var(--brand) per tenant
- Font: Plus Jakarta Sans only
- PHES brand color: #0065A5
```

That single paste gives me full context instantly. No re-explaining. No screenshots of what's built. We go straight to work.

---

**On how big this undertaking is — honest answer:**

This is a legitimate SaaS product. Not a side project. Here's the full picture:

| Category | Status | Complexity |
|---|---|---|
| Core platform | 60% built | Done |
| Operational features | 30% built | High |
| Financial features | 20% built | Very High |
| Client-facing features | 5% built | Medium |
| Integrations | 10% built | Very High |

You're building what would cost $180k-$400k from a dev agency. With Replit + AI you're doing it for under $100/month. The trade-off is time and iteration cycles.

Realistic timeline to first paying customer: **4-6 more weeks** of consistent daily build sessions. You have the hardest parts done — multi-tenant architecture, auth, and UI system. What's left is features, not foundation.

Realistic timeline to full feature parity with MaidCentral: **4-5 months**. But you don't need full parity to charge. You need about 60% of what's left.

---

**Geo-fencing prompt — paste this into Replit now:**

```
Build the complete geo-fencing clock-in/out system.
This is the most critical operational feature.
Build in this exact order — show me each step before 
moving to the next.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1 — DATABASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run these SQL statements:

CREATE TABLE IF NOT EXISTS time_clock_entries (
  id                    SERIAL PRIMARY KEY,
  company_id            INTEGER NOT NULL REFERENCES companies(id),
  job_id                INTEGER NOT NULL REFERENCES jobs(id),
  user_id               INTEGER NOT NULL REFERENCES users(id),
  clock_in_at           TIMESTAMP,
  clock_out_at          TIMESTAMP,
  clock_in_lat          DECIMAL(10,8),
  clock_in_lng          DECIMAL(11,8),
  clock_out_lat         DECIMAL(10,8),
  clock_out_lng         DECIMAL(11,8),
  clock_in_distance_ft  INTEGER,
  clock_out_distance_ft INTEGER,
  is_flagged            BOOLEAN DEFAULT false,
  flag_reason           TEXT,
  created_at            TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS job_photos (
  id          SERIAL PRIMARY KEY,
  company_id  INTEGER NOT NULL REFERENCES companies(id),
  job_id      INTEGER NOT NULL REFERENCES jobs(id),
  user_id     INTEGER NOT NULL REFERENCES users(id),
  photo_type  VARCHAR(10) NOT NULL 
              CHECK (photo_type IN ('before','after')),
  url         TEXT NOT NULL,
  lat         DECIMAL(10,8),
  lng         DECIMAL(11,8),
  taken_at    TIMESTAMP DEFAULT NOW()
);

ALTER TABLE jobs 
  ADD COLUMN IF NOT EXISTS lat DECIMAL(10,8),
  ADD COLUMN IF NOT EXISTS lng DECIMAL(11,8);

Show me confirmation these tables exist before 
moving to Step 2.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2 — API ROUTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add these routes. All require auth and filter by 
req.auth.companyId for tenant isolation.

Haversine distance function (add to a utils file):
function haversineDistanceFt(lat1, lng1, lat2, lng2) {
  const R = 20902461;
  const dLat = (lat2-lat1) * Math.PI/180;
  const dLng = (lng2-lng1) * Math.PI/180;
  const a = Math.sin(dLat/2)*Math.sin(dLat/2) +
    Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*
    Math.sin(dLng/2)*Math.sin(dLng/2);
  return Math.round(20902461 * 2*Math.atan2(
    Math.sqrt(a),Math.sqrt(1-a)));
}

GET /api/jobs/my-jobs
  Returns jobs for today assigned to req.auth.userId.
  Include per job:
    id, client_name, address, lat, lng,
    service_type, scheduled_time, status, amount,
    client_notes,
    time_clock_entry (current entry for this job+user),
    before_photo_count, after_photo_count

POST /api/clock/in
  Body: { job_id, lat, lng }
  1. Fetch job, get job.lat + job.lng
  2. Calculate distance with haversineDistanceFt
  3. is_flagged = distance > company.geo_fence_threshold_ft
  4. Insert time_clock_entries record
  5. Update job status = 'in_progress'
  6. Return { entry_id, distance_ft, is_flagged, 
              flag_reason, clock_in_at }

POST /api/clock/out
  Body: { entry_id, lat, lng }
  1. Verify entry belongs to req.auth.userId + companyId
  2. Count after photos for this job —
     if count = 0, return 400:
     { error: 'PHOTOS_REQUIRED',
       message: 'Upload at least 1 after photo first' }
  3. Calculate distance
  4. Update entry: clock_out_at, lat, lng, distance
  5. Update job status = 'complete'
  6. Return { clock_out_at, duration_minutes }

POST /api/jobs/:jobId/photos
  Multipart upload. Fields: photo_type, lat, lng
  1. Validate: jpeg/png/webp only, max 10MB
  2. Save to uploads/photos/{companyId}/{jobId}/
  3. Insert job_photos record
  4. Return { photo_id, url, photo_type }

GET /api/jobs/:jobId/photos
  Returns { before: [...urls], after: [...urls] }
  Filtered by company_id.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3 — MY JOBS MOBILE PAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create src/pages/my-jobs.tsx
Add to App.tsx routes: /my-jobs
Add to sidebar under OPERATIONS: 
  "My Jobs" with ClipboardList icon — visible to ALL roles

This page is mobile-first. Max-width 420px centered.
Uses the light theme color system.

STICKY HEADER:
  bg #FFFFFF, border-bottom 1px solid #E5E2DC
  Left: user initials avatar (28px, var(--brand-dim) bg,
        var(--brand) text)
  Center: "My Jobs" 15px weight 600 #1A1917
  Right: today's date 12px #6B6860

GEO STATUS PILL (below header, centered):
  Location not yet requested:
    bg #F3F4F6, text #6B7280
    "Tap clock-in to enable location"
  
  Within geo-fence (<= threshold):
    bg #DCFCE7, text #166534
    Green dot + "In zone — [X]ft from job"
  
  Outside geo-fence:
    bg #FEF3C7, text #92400E  
    Amber dot + "Out of zone — [X]ft from job"

JOB CARDS — for each job today:

Active job card (scheduled or in_progress):
  bg #FFFFFF, border 1px solid #E5E2DC
  border-left: 3px solid var(--brand)
  border-radius: 12px
  padding: 18px
  margin: 12px

  TOP: status badge + "$amount" right (20px weight 700)
  Client name: 18px weight 700 #1A1917, margin-top 10px
  Service type: 11px weight 600 var(--brand) uppercase
  Time: 12px #6B6860
  Address: 12px #6B6860

  CLIENT NOTES (if exists):
    bg #F7F6F3, border-radius 8px, padding 10px
    "Client notes" label 10px #9E9B94 uppercase
    Notes text 12px #1A1917

  BEFORE PHOTOS SECTION:
    "Before Photos" label 12px weight 600 #1A1917
    2-column photo grid, gap 8px:
      Uploaded: 72x72 thumbnail, border-radius 8px
      Add button: 72x72 dashed border 1.5px #E5E2DC
                  bg #F7F6F3, "+" 20px #9E9B94
                  tap = opens file picker
                  accept="image/*" capture="environment"

  AFTER PHOTOS SECTION:
    Same layout as before photos
    
    If in_progress AND after_count = 0:
      Warning box: bg #FEF3C7, border-left 3px #F59E0B
      "After photos required before clock-out"
      12px #92400E

  CLOCK BUTTON:

  NOT clocked in:
    Full-width button
    bg var(--brand), color #FFFFFF
    "Clock In" 15px weight 600
    padding 16px, border-radius 10px
    border: none
    
    On tap:
      navigator.geolocation.getCurrentPosition(
        success, error, { enableHighAccuracy: true }
      )
      Loading state: spinner + "Getting location..."
      POST /api/clock/in
      If is_flagged: show amber toast with distance
      On success: refresh job data, show elapsed timer

  Clocked in (has entry, no clock_out_at):
    Elapsed timer display:
      "1:23:45" — 36px weight 700 #1A1917
                  font-variant-numeric: tabular-nums
      "Time on job" — 11px #9E9B94
    
    Clock Out button:
      after_count > 0:
        bg #166534, color #FFFFFF
        "Clock Out" 15px weight 600
      after_count = 0:
        bg #F3F4F6, color #9E9B94
        "Clock Out — add after photo first"
        pointer-events: none
    
    On clock-out success:
      Show completion state:
        Large checkmark in var(--brand)
        "Job Complete" 18px weight 700 #1A1917
        "Duration: 2h 15m" 14px #6B6860

UPCOMING JOBS (below active):
  "Up Next" 11px #9E9B94 uppercase, margin-top 20px
  Same card style but opacity 0.55
  No clock button — just shows time and client

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4 — CLOCK MONITOR (Admin view)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create src/pages/clock-monitor.tsx
Route: /employees/clocks
Add to sidebar under Employees (owners/admins only):
  "Clock Monitor" with Clock icon

Page title: "Clock Monitor"
Sub: "Today's clock activity across all employees"

TABLE columns:
  EMPLOYEE | JOB | CLOCK IN | CLOCK OUT | 
  DURATION | DISTANCE | STATUS

Status badges:
  On Job: green
  Complete: blue  
  Flagged: red

Flagged rows: 
  bg rgba(239, 68, 68, 0.04)
  border-left: 3px solid #EF4444

Clicking flagged row opens modal:
  Employee name + job
  Clock in time + distance in feet
  "X feet from [job address]"
  Two buttons: "Dismiss Flag" | "Keep Flag"
  Dismiss: sets is_flagged = false in DB

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 5 — GEOCODE EXISTING JOBS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add geocoding to the job creation/update flow.
When a job is saved with an address, automatically 
call Google Maps Geocoding API to get lat/lng.

Add to .env: GOOGLE_MAPS_API_KEY=

In job create/update route:
async function geocodeAddress(address) {
  const key = process.env.GOOGLE_MAPS_API_KEY;
  const url = `https://maps.googleapis.com/maps/api/geocode/json?address=${encodeURIComponent(address)}&key=${key}`;
  const res = await fetch(url);
  const data = await res.json();
  if (data.results?.[0]) {
    return {
      lat: data.results[0].geometry.location.lat,
      lng: data.results[0].geometry.location.lng
    };
  }
  return null;
}

For existing jobs with no lat/lng, add admin endpoint:
POST /api/admin/geocode-jobs (super_admin only)
  Loops all jobs with address but null lat/lng,
  geocodes each, updates record.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AFTER ALL 5 STEPS — VERIFY:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. /my-jobs loads and shows today's assigned jobs
2. Clock In requests browser geolocation
3. Distance calculates and shows in geo pill
4. Before photo uploads and shows in grid
5. Clock Out is DISABLED until after photo uploaded
6. After clock-out: job status = complete
7. /employees/clocks shows entry with distance
8. Flagged entries show red badge
9. Push all changes to GitHub when complete

Build step by step. Show confirmation after 
each step before proceeding.
```

---

That's everything needed to complete the most critical feature in the platform. Once geo clock-in works, every downstream system has real data — payroll calculations, scorecard entries, job completion reports, dashboard metrics. It unlocks everything.