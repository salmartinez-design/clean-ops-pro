Correct — the original prompt references templates but never fully builds them, and now every template also needs branch-aware variables baked in. I'm merging everything into one complete, paste-ready prompt.

---

**PROMPT — COMPLETE BOOKING SUBMISSION FLOW WITH BRANCH ROUTING AND ALL EMAIL TEMPLATES**

---

**CRITICAL — COMMUNICATIONS SAFETY NOTE**

Outbound communications are currently paused in Qleno. This prompt must NOT re-enable or override that pause.

1. Do NOT change any `communications_paused`, `notifications_enabled`, or similar flag in the database or settings — leave all existing communication toggle states exactly as they are.
2. Every email and SMS send in this prompt must check the communications pause flag first. If paused, log the intended send with full details but do not send.
3. Do NOT trigger any existing automations, Zapier connections, follow-up sequences, or MC-connected workflows as a side effect of this build.
4. No Twilio number may fire any outbound SMS during this build. All SMS sends must be gated behind the pause flag check.
5. For the verification test, emails to salmartinez@phes.io and info@phes.io are acceptable only if Sal explicitly unpauses communications and approves a single test send. Do not send automatically during the build.

After completing the build, report: the current pause flag state, where the flag lives, confirmation that all sends check the flag, and the exact steps to safely re-enable when ready.

---

**STEP 0 — ZIP-TO-BRANCH ROUTING UTILITY**

Before building any booking logic, create a shared utility function `getBranchByZip(zip)` in `lib/branchRouter.ts`. This function must be importable by the booking handler, quote builder, and cron job. Every email address, phone number, and Twilio number used anywhere in this prompt must come from this function's return value — never hardcoded.

```typescript
const SCHAUMBURG_ZIPS = new Set([
  '60010','60011','60107','60120','60172','60179','60192','60201',
  '60009','60017','60019','60089','60090','60007',
  '60159','60168','60169','60173','60193','60194','60195','60196',
  '60004','60005','60006','60008','60038','60055','60056','60067',
  '60074','60078','60094','60095'
]);

export function getBranchByZip(zip: string) {
  const isSchaumburg = SCHAUMBURG_ZIPS.has(zip?.toString().trim());
  if (isSchaumburg) {
    return {
      branch: 'schaumburg',
      officeEmail: 'schaumburg@phes.io',
      fromName: 'Phes',
      clientPhone: '847-538-3729',
      clientPhoneFormatted: '(847) 538-3729',
      twilioFrom: '+16308844318',
    };
  }
  return {
    branch: 'oak_lawn',
    officeEmail: 'info@phes.io',
    fromName: 'Phes',
    clientPhone: '773-706-6000',
    clientPhoneFormatted: '(773) 706-6000',
    twilioFrom: '+17737869902',
  };
}

export async function getCompanyIdByBranch(branch: string): Promise<string> {
  // Query companies table
  // 'schaumburg' → WHERE name ILIKE '%schaumburg%'
  // 'oak_lawn'   → WHERE name ILIKE '%oak lawn%' OR name = 'Phes'
  // Return company.id
}
```

Call `getBranchByZip(zip)` immediately after the zip code is read from any form submission. Store the result as `branchConfig`. Use `branchConfig.officeEmail`, `branchConfig.clientPhone`, `branchConfig.clientPhoneFormatted`, and `branchConfig.twilioFrom` for every downstream operation. Never use string literals for these values.

---

**STEP 1 — STRIPE SETUP INTENT**

When "Confirm & Book" is clicked on Step 5 of the booking widget:

1. Call `getBranchByZip(zip)` immediately. Store as `branchConfig`. Call `getCompanyIdByBranch(branchConfig.branch)` and store as `companyId`.

2. Create a Stripe Customer if one does not already exist for this email. Store the Stripe customer ID on the client record.

3. Create a SetupIntent attached to the Stripe customer using the payment method from the Stripe Elements card field.

4. Confirm the SetupIntent. If it fails: show inline error above the card field — "We were unable to save your card. Please check your card details and try again." Do NOT create any client or job records. Do NOT send any emails. Allow the client to correct and resubmit.

5. If SetupIntent succeeds: store the payment_method ID and Stripe customer ID on the client record. Set `payment_source = 'stripe'`. Proceed to Step 2.

Use STRIPE_SECRET_KEY and STRIPE_PUBLISHABLE_KEY already in Replit Secrets.

---

**STEP 2 — CLIENT RECORD CREATION**

Check if a client with the submitted email already exists in the clients table.

If client EXISTS: update `phone`, `service_address`, `zip_code` if changed. Do not overwrite name or create a duplicate. Use the existing `client_id` for job creation.

If client does NOT exist, create a new client record with:
- `first_name`, `last_name`, `email`, `phone`, `zip_code`, `service_address`, `unit_suite_access`, `acquisition_source`
- `stripe_customer_id`, `stripe_payment_method_id`
- `payment_source = 'stripe'`
- `status = 'active'`
- `client_since = today`
- `preferred_contact_method`
- `company_id = companyId` (from branchConfig — NOT hardcoded)

---

**STEP 3 — JOB RECORD CREATION**

Run these migrations first if columns do not exist:

```sql
ALTER TABLE jobs ADD COLUMN IF NOT EXISTS reminder_72h_sent boolean DEFAULT false;
ALTER TABLE jobs ADD COLUMN IF NOT EXISTS reminder_24h_sent boolean DEFAULT false;
ALTER TABLE jobs ADD COLUMN IF NOT EXISTS branch varchar(50) DEFAULT 'oak_lawn';
ALTER TABLE quotes ADD COLUMN IF NOT EXISTS branch varchar(50) DEFAULT 'oak_lawn';
```

Create a job record with:
- `client_id`, `company_id = companyId`, `branch = branchConfig.branch`
- `service_type`, `scope`, `sqft`, `bedrooms`, `full_bathrooms`, `half_bathrooms`, `floors`, `people_in_household`, `pets`, `cleanliness_rating`
- `addons` (array of selected add-on IDs and names)
- `bundle_discount` (dollar amount or 0)
- `special_notes`, `scheduled_date`, `arrival_window`, `preferred_contact_method`
- `first_visit_total`
- `status = 'scheduled'`
- `payment_status = 'unpaid'`
- `payment_source = 'stripe'`
- `created_at = now()`
- `service_address`, `unit_suite_access`
- `reminder_72h_sent = false`, `reminder_24h_sent = false`

The job must appear immediately on the dispatch board at /dispatch under the correct scheduled date with status "Scheduled".

---

**STEP 4 — CONFIRMATION EMAIL TO CLIENT**

Check the communications pause flag first. If paused, log all details and do not send.

If active, send via Resend using RESEND_API_KEY.

```
From: Phes <{branchConfig.officeEmail}>
Reply-To: {branchConfig.officeEmail}
```

Route subject line by service_type:
- Deep Clean → "Your Phes Deep Clean is Confirmed"
- One-Time Standard Clean → "Your Phes Cleaning is Confirmed"
- Move In / Move Out → "Your Phes Move In/Out Clean is Confirmed"
- Recurring → "Your Phes Recurring Service is Confirmed"

Build all four templates as clean HTML. Phes brand color #5B9BD5 for headers and accents. Plus Jakarta Sans with Arial fallback. Max width 600px centered. White background. Every phone number and email address in every template must use `branchConfig.clientPhone`, `branchConfig.clientPhoneFormatted`, and `branchConfig.officeEmail` — never hardcoded strings.

---

**TEMPLATE STRUCTURE (apply to all four templates with noted variations)**

**APPOINTMENT SUMMARY BLOCK**

```html
<div style="background:#EBF4FF;border-left:4px solid #5B9BD5;padding:16px;margin-bottom:24px;">
  <strong>Your Appointment Details</strong><br><br>
  Service: {service_type}<br>
  Date: {formatted as "Tuesday, April 14, 2026"}<br>
  Arrival Window: {9:00 AM - 12:00 PM | 12:00 PM - 2:00 PM}<br>
  Address: {service_address} {unit_suite_access if provided}<br>
  Preferred Contact: {preferred_contact_method}<br><br>
  Our office will reach out via {preferred_contact_method} to confirm your exact 
  arrival time within your window.<br><br>
  You will also receive automatic reminders:<br>
  - 72 hours before your appointment via SMS and email<br>
  - 24 hours before your appointment via SMS and email
</div>
```

**GREETING**

{first_name}, thank you for your business!

Your cleaning is officially scheduled! We are excited to help you refresh your home. Please review the details of your appointment and our service policies below to ensure everything goes smoothly.

**WHAT HAPPENS NEXT**

What Happens Next:
1. Our office will contact you via {preferred_contact_method} to confirm your exact arrival time within your window
2. You will receive an SMS and email reminder 72 hours before your appointment
3. You will receive an SMS and email reminder 24 hours before your appointment
4. Your technician will arrive during your selected window
5. After your cleaning you will receive a satisfaction follow-up from our team
6. Your card on file will be charged upon job completion

**CANCELLATION & RESCHEDULING POLICY**

Cancellation & Rescheduling Policy

We reserve time specifically for your home. To protect our staff's wages, we enforce the following:

48-Hour Notice: Cancellations/rescheduling must be made 48 business hours in advance. Sundays do not count.

Monday Appts: Notify us by Friday before 6:00 PM CT.
Tuesday Appts: Notify us by Saturday before 12:00 PM CT.

Fees: Cancellations within 48 hours or No-Shows result in a 100% charge of the service fee.

Rescheduling: Clients are allowed ONE reschedule per appointment. Any additional reschedule request will be treated as a late cancellation and will incur a 100% fee regardless of whether the request is made within or outside the 48-hour window.

Lockouts: Our team will wait a maximum of 20 minutes. If we cannot gain access and cannot reach you, the appointment is forfeited and billed in full.

**SITE REQUIREMENTS**

Site Requirements & Preparation

Utilities: Running water, electricity, and sufficient lighting must be available. If utilities are inactive, we reserve the right to cancel, and the full fee will still apply.

Declutter: Please have personal items, toys, and clothes cleared away. We cannot clean sinks or countertops full of dishes. Highly cluttered surfaces may be skipped at our discretion.

Renovations: Please disclose if your home has recently undergone construction/renovation, as this requires specific post-construction pricing.

[DEEP CLEAN / ONE-TIME STANDARD / RECURRING ONLY — omit from Move In/Out:]
Maintenance Clients: Please provide a toilet brush for our team to use inside your toilets.

[MOVE IN/OUT ONLY — omit from all others:]
Move-In/Out Clients: Property must be empty of furniture and people. We will work around any items left behind, which may result in subpar cleaning; no refunds will be issued for these conditions.

Hourly service: We bill upon the start of the job the minimum of 3 hours for standard cleaning and 4 hours for deep or move-in-out cleaning. Any additional time needed will be communicated to you and billed to the card on file at the end of service.

**SAFETY, LIABILITY & EXCLUSIONS**

Safety, Liability & Exclusions

Biohazards: Per OSHA guidelines, we do not clean human/animal waste (feces, urine, vomit), blood, or insect infestations.

Climate: Cleaners are authorized to adjust AC/Heat to a safe working temperature while on-site.

Damage Cap: Our liability for any damage is limited to the total cost of the cleaning service. We are not responsible for improperly secured items (loose shelves/frames) or items of extreme sentimental value.

Exclusions: We do not offer bed-making, laundry, dishwashing, wall spot-cleaning, or moving heavy furniture.

**24-HOUR GUARANTEE**

The 24-Hour Right to Rectify

Our Guarantee: If we miss a spot, contact us within 24 hours. We will return to re-clean the area at no cost.

No Refunds: As a labor-based service, we do not offer refunds. The re-clean is our sole remedy for quality disputes.

**HOME ACCESS OPTIONS**

Home Access Options

How will we be entering your home?
1. Be Home: Wait for our arrival during the window.
2. Keys/Codes: Provide us with a spare key or electronic entry code.
3. Secure Lockbox: We can provide a master lockbox for $50.00. Must be returned upon termination of service or a $75.00 fee applies.

**NON-SOLICITATION**

Non-Solicitation Agreement

Our staff is our greatest asset. By using our services, you agree not to solicit, hire, or contract any Phes staff member privately. Any breach will result in the immediate termination of your service agreement.

**PRICING NOTE**

Pricing Note: Flat Rate estimates are based on your selections. If the home's condition differs significantly, we will provide an updated estimate. Extra time is billed at $65/hour per cleaner.

Review our full Terms and Conditions at phes.io/terms and Privacy Policy at phes.io/privacy-policy

**RECURRING SERVICE UPSELL**

[INCLUDE FOR DEEP CLEAN AND ONE-TIME STANDARD ONLY — omit for Recurring and Move In/Out]

Interested in Regular Cleaning Service?

Clients on a recurring schedule receive preferred scheduling, consistent technician assignment, and discounted rates. Weekly, biweekly, and monthly plans are available.

Call us at {branchConfig.clientPhoneFormatted} or reply to this email to set up your recurring service today.

**CLIENT DETAILS**

Please verify the information below and contact us at {branchConfig.clientPhoneFormatted} or {branchConfig.officeEmail} if you have any concerns or questions. Thank you again for your business.

Be sure to check out phes.io to find out about all the great services Phes offers!

Address: {service_address} {unit_suite_access if provided}
Payment Method: Card on file
Preferred Contact: {preferred_contact_method}
Email: {client_email}
Phone: {client_phone}

**SERVICE DETAILS TABLE**

"We've itemized the services we are providing."

Render as a clean HTML table:

```html
<table style="width:100%;border-collapse:collapse;">
  <tr>
    <td>{service_type} - {sqft} sqft</td>
    <td align="right">${base_price}</td>
  </tr>
  <!-- repeat for each addon -->
  <tr>
    <td>{addon_name}</td>
    <td align="right">+${addon_price}</td>
  </tr>
  <!-- only if bundle_discount > 0 -->
  <tr>
    <td style="color:#2D6A4F;">Appliance Bundle Discount</td>
    <td align="right" style="color:#2D6A4F;">-${bundle_discount}</td>
  </tr>
  <tr>
    <td><strong>First Visit Total</strong></td>
    <td align="right"><strong>${first_visit_total}</strong></td>
  </tr>
</table>
```

**FOOTER**

```
Phes
{branchConfig.clientPhoneFormatted}
{branchConfig.officeEmail}
phes.io
```

---

**STEP 5 — OFFICE NOTIFICATION EMAIL**

Check communications pause flag first. If paused, log and do not send.

```
To: {branchConfig.officeEmail}
From: Phes <{branchConfig.officeEmail}>
Reply-To: {branchConfig.officeEmail}
Subject: New Booking — {first_name} {last_name} — {service_type} — {scheduled_date formatted}
```

Body:

```
NEW ONLINE BOOKING

Client: {full_name}
Phone: {phone}
Email: {email}
Branch: {branchConfig.branch}
Service: {service_type}
Date: {scheduled_date}
Arrival Window: {arrival_window}
Address: {service_address}
Unit/Access: {unit_suite_access or "Not provided"}
Square Footage: {sqft}
Bedrooms: {bedrooms}
Bathrooms: {full_bathrooms} full / {half_bathrooms} half
Floors: {floors}
People in Household: {people_in_household}
Pets: {pets}
Cleanliness Rating: {1-Very Clean / 2-Moderately Clean / 3-Very Dirty}
Add-ons: {comma separated or "None"}
Bundle Discount: {$amount or "N/A"}
Special Notes: {special_notes or "None"}
Preferred Contact: {preferred_contact_method}
How They Found Us: {acquisition_source}
First Visit Total: ${first_visit_total}
Stripe Customer ID: {stripe_customer_id}
Stripe Payment Method: {stripe_payment_method_id}
```

---

**STEP 6 — BOOKING CONFIRMATION SCREEN**

After all steps complete, show a final confirmation screen in place of the payment form. Do not redirect.

```
[Checkmark icon in #5B9BD5]

Your booking is confirmed!

Thank you, {first_name}. Your {service_type} is scheduled for {formatted date} 
with a {arrival_window} arrival window.

A confirmation email has been sent to {email}.

Our office will reach out via {preferred_contact_method} to confirm your exact 
arrival time. You will also receive SMS and email reminders 72 hours and 24 hours 
before your appointment.

Questions? Call us at {branchConfig.clientPhoneFormatted} or email 
{branchConfig.officeEmail}
```

---

**STEP 7 — AUTOMATED REMINDER SEQUENCE**

Add reminder checks to the existing nightly 2AM cron. All sends gated behind the communications pause flag.

For each job being processed, call `getBranchByZip(job.zip_code)` to get `branchConfig` for that specific job. Do not use any hardcoded phone number or Twilio number.

**REMINDER 1 — 72 Hours Before Appointment**

Query jobs where `status = 'scheduled'`, `scheduled_date` is between 71–73 hours from now, `reminder_72h_sent = false`. Skip jobs where status is `cancelled`, `void`, or `done`.

SMS (check pause flag first — if paused, log and skip):
```
From: {branchConfig.twilioFrom}

Hi {first_name}, this is Phes confirming your cleaning appointment on {day, date} 
with a {arrival_window} arrival window at {service_address}. Questions? Call us at 
{branchConfig.clientPhone}. Reply STOP to unsubscribe.
```

Email (check pause flag first — if paused, log and skip):
```
From: Phes <{branchConfig.officeEmail}>
Subject: Reminder: Your Phes Cleaning is in 3 Days
```

Email body:

[Appointment summary block — same structure as Step 4, with service, date, window, address]

Hi {first_name}, this is a friendly reminder that your Phes cleaning is coming up in 3 days.

If you need to reschedule, please contact us at least 48 business hours before your appointment. Remember — Sundays do not count toward this window.

Monday appointments: notify us by Friday before 6:00 PM CT.
Tuesday appointments: notify us by Saturday before 12:00 PM CT.

Contact us: {branchConfig.clientPhoneFormatted} or {branchConfig.officeEmail}

Footer: Phes / {branchConfig.clientPhoneFormatted} / {branchConfig.officeEmail}

After sending (or logging if paused): set `reminder_72h_sent = true`.

**REMINDER 2 — 24 Hours Before Appointment**

Query jobs where `status = 'scheduled'`, `scheduled_date` is between 23–25 hours from now, `reminder_24h_sent = false`. Skip cancelled, void, done.

SMS (check pause flag first — if paused, log and skip):
```
From: {branchConfig.twilioFrom}

Hi {first_name}, your Phes cleaning is tomorrow with a {arrival_window} arrival 
window at {service_address}. Please ensure access to your home is available. 
Questions? Call {branchConfig.clientPhone}. Reply STOP to unsubscribe.
```

Email (check pause flag first — if paused, log and skip):
```
From: Phes <{branchConfig.officeEmail}>
Subject: Reminder: Your Phes Cleaning is Tomorrow
```

Email body:

[Appointment summary block — service, date, window, address]

Hi {first_name}, your Phes cleaning is tomorrow!

Your team will arrive during your {arrival_window} window. Please ensure your home is accessible and all utilities are active — running water, electricity, and sufficient lighting must be available.

Home Access Reminder:
- Be home during your arrival window, OR
- Ensure your key, entry code, or lockbox is ready for our team

Questions or need to make a change? Contact us immediately at {branchConfig.clientPhoneFormatted} or {branchConfig.officeEmail} — please note our 48-hour cancellation policy applies.

Footer: Phes / {branchConfig.clientPhoneFormatted} / {branchConfig.officeEmail}

After sending (or logging if paused): set `reminder_24h_sent = true`.

---

**STEP 8 — ERROR HANDLING**

If Stripe SetupIntent fails: show inline error, stop all processing, do not create records or send emails.

If job record creation fails after Stripe succeeds: log error with SetupIntent ID and client email. Send office error notification to `branchConfig.officeEmail` with subject "ERROR - MANUAL ACTION REQUIRED - Failed Booking - {client_email}" (check pause flag first). Show confirmation screen to client anyway.

If client confirmation email fails: log failure with job_id and client email. Do not block the booking.

If office notification email fails: log failure with job_id. Do not block the booking.

If reminder SMS or email fails: log failure with job_id and error. Do not mark reminder as sent. Cron retries on next run.

Never show a technical error to the client after card authorization succeeds.

---

**STEP 9 — QUOTE BUILDER BRANCH TAGGING**

In the internal quote builder: when a quote is created or saved, call `getBranchByZip(client.zip_code)` and set `branch = branchConfig.branch` on the quote record. If the quote builder sends outbound emails, apply the same branchConfig rules — From address, phone numbers in body, and office routing all come from branchConfig. If it does not yet send emails, apply the branch tag only.

---

**STEP 10 — COMMUNICATIONS PAUSE FLAG REPORT**

Report:
1. Current pause flag state — paused or active
2. Where the flag lives — table, column name, or environment variable
3. Confirmation that every email and SMS send in Steps 4, 5, and 7 checks this flag before sending
4. Exact steps to safely re-enable communications when ready

---

**STEP 11 — VERIFICATION REPORT**

Report PASS or FAIL for each:

1. `getBranchByZip('60193')` returns branch `schaumburg`, phone `847-538-3729`, twilioFrom `+16308844318`, officeEmail `schaumburg@phes.io`
2. `getBranchByZip('60453')` returns branch `oak_lawn`, phone `773-706-6000`, twilioFrom `+17737869902`, officeEmail `info@phes.io`
3. Stripe SetupIntent flow wired correctly
4. Client record creation correct including duplicate email check
5. `company_id` on client and job records resolved dynamically from companies table via branchConfig — not hardcoded
6. Job record created with all fields including `branch`, `reminder_72h_sent`, `reminder_24h_sent`
7. Job appears on dispatch board immediately after booking
8. All four email templates built (Deep Clean, One-Time Standard, Move In/Out, Recurring)
9. Recurring upsell present in Deep Clean and One-Time Standard templates only — omitted from Recurring and Move In/Out
10. Move In/Out specific language correct, toilet brush line omitted
11. All phone numbers and emails in all templates use branchConfig variables — no hardcoded strings
12. Office notification routes to `branchConfig.officeEmail`
13. Booking confirmation screen shows `branchConfig.clientPhoneFormatted` and `branchConfig.officeEmail`
14. Reminder cron calls `getBranchByZip` per job — no hardcoded Twilio number
15. All sends gated behind communications pause flag
16. `branch` column added to `jobs` table
17. `branch` column added to `quotes` table
18. `reminder_72h_sent` and `reminder_24h_sent` columns exist on jobs table
19. Error handling in place for all failure scenarios
20. Communications pause flag location documented and re-enable steps provided

Do NOT run a live test booking. Report build status only. Sal will manually trigger the test when ready.