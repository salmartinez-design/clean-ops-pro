```
Fix the Dispatch Board layout. The circled area in the 
screenshot shows the problem — the Team Today panel on 
the left is a duplicate of the technician column in the 
Gantt and is wasting significant horizontal space.

=== FIX 1 — REMOVE TEAM TODAY PANEL ===

The left sidebar panel showing "Team Today" with the 
list of technicians (AC Alejandra Cuervo, AS Alma Salinas, 
AV Ana Valdez, etc.) must be removed entirely from the 
Dispatch Board.

The Gantt already has a TECHNICIAN column on the left 
side of the timeline — that is the correct and only 
place technicians should appear. Having both is 
redundant and cuts the usable schedule space in half.

Remove the Team Today sidebar panel completely.
Do not remove it from the Dashboard — only from 
the Dispatch Board page at /dispatch.

=== FIX 2 — EXPAND GANTT TO USE FULL WIDTH ===

With Team Today removed, the Gantt chart should now 
expand to fill the full available width of the page 
(minus the main sidebar nav).

The TECHNICIAN column on the left of the Gantt 
should remain — this is the single source of 
truth for who is on the schedule today.

Minimum column widths:
  Technician column: 180px (enough for name + role + stats)
  Each hour column: 80px minimum
  The timeline should stretch to fill remaining space

=== FIX 3 — START TIMELINE AT 9AM ===

The visible timeline currently starts at 7:00 AM 
creating dead space before the first job.

Two changes required:
1. Set the initial scroll position so 9:00 AM is 
   the leftmost visible hour when the board loads.
   Calculate: scrollLeft = (9 - startHour) * columnWidth
   Apply on component mount and on date change.

2. The timeline should still render from 7:00 AM 
   (or the earliest job time if before 9am) so 
   early jobs are accessible by scrolling left — 
   but 9am must be the default visible start.

=== FIX 4 — TECHNICIAN COLUMN IMPROVEMENTS ===

Keep the existing technician column but tighten it:

Each technician row should show:
  - Avatar circle with initials (existing)
  - Full name (bold)
  - Role badge (TECHNICIAN / OFFICE — existing)
  - Job count · Hours · Revenue for today
    (existing data, just keep it)

Remove any dot indicators that aren't wired to 
real data yet — only show the green dot if the 
technician is actively clocked in right now.

Row height: enough for the content but no excess 
padding. Aim for ~64px per row, consistent across 
all technicians.

=== FIX 5 — GENERAL SPACING CLEANUP ===

- Remove any left margin or padding before the 
  technician column that creates the green dead zone
- The board should start flush after the main nav sidebar
- Ensure the calendar mini-picker and date nav stay 
  in the existing top bar — do not move them
- Zone filter pills stay in the top bar as-is
- "+ New Job" button stays top left as-is

=== RESULT EXPECTED ===

Before: [Nav] [Team Today panel 200px] [Technician col] [Timeline — cramped]
After:  [Nav] [Technician col 180px] [Timeline — full width from 9am]

Design: #F7F6F3 background, #FFFFFF cards, 
Plus Jakarta Sans, no emojis, borders #E5E2DC.
Confirm clean build and publish.
```