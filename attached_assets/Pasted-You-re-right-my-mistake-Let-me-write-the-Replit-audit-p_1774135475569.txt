You're right, my mistake. Let me write the Replit audit prompts you can paste directly into the agent.

---

## Audit Prompt 1 — Unconfirmed Pages

Paste this into Replit:

---

**AUDIT ONLY — DO NOT CHANGE ANY CODE.**

I need a full audit of the following pages in the Qleno codebase. For each item, tell me exactly what exists in the code — what component file handles it, what routes are registered, what data it fetches, and whether it is functional, partially built, or missing entirely. Do not fix anything. Do not suggest fixes. Just report what is there.

**1. Jobs list page**
- What route renders it?
- What component file?
- What filters/tabs exist?
- What columns/fields show on each job row?

**2. Job detail page**
- What route renders it?
- What component file?
- What sections/tabs exist on the detail view?
- Is there a recurrence indicator anywhere on the detail?

**3. New Job wizard**
- What component handles it?
- How many steps/tabs does it have?
- List every field on every step.
- Is there a recurrence option anywhere in the wizard?

**4. Invoice detail page**
- What route renders it?
- What component file?
- What does it display?
- Is the New Invoice button wired to a handler? What does it call?

**5. Company Settings — list every tab that exists**
- General tab — what fields are on it?
- Notifications tab — what fields are on it?
- Clock In/Out tab — what fields are on it?
- Invoicing tab — what fields are on it?
- Integrations tab — what fields are on it?
- Payroll Options tab — what fields are on it?
- HR Policies tab — what fields are on it? Does it render a form or is it a blank/stub?

**6. Core KPIs page**
- Does a standalone Core KPIs page exist?
- What route? What component? What does it display?

**7. Super Admin portal (/admin)**
- What route handles /admin?
- What component renders it?
- List every section and feature visible in the portal.

**8. My Jobs mobile view**
- What component handles it?
- What fields/data show on each job card from the technician perspective?
- Is GPS clock-in/out wired to this view?

**9. Cleancyclopedia**
- What route and component?
- Is content bilingual (EN/ES)?
- How many articles or entries exist?

Report findings for each item numbered 1–9. Be specific about file names and component names.

---

## Audit Prompt 2 — Unconfirmed Features

Paste this as a second prompt after the first completes:

---

**AUDIT ONLY — DO NOT CHANGE ANY CODE.**

I need a status report on the following features. For each one, tell me: does it exist in the codebase, is it fully wired end-to-end, or is it a stub/partial build? Include the relevant file names.

**1. Qleno rebrand**
- Does the sidebar logo reference "Qleno" or "CleanOps Pro"?
- What is the browser tab title set to across pages?
- Is there a favicon file and is it referenced in the HTML?
- What are the component file names: QlenoMark.jsx and QlenoLogo.jsx — do they exist?

**2. Recurring Jobs**
- Is there a recurrence field on the job creation wizard?
- Is there a `recurrence_rule` or equivalent column on the jobs table in Supabase?
- Does a recurring job generate child/future job instances? If so, where is that logic?

**3. Cancellation / Reschedule Workflow**
- Are Cancel and Reschedule actions available on a job detail?
- Is there a cancellation reason prompt or modal?
- Is there a reschedule date picker flow?
- Is any of this logged to an audit or communication table?

**4. Client Communication Log**
- Does a communication log or activity tab exist on the customer detail page?
- What table does it read from?
- What types of entries does it log?

**5. Incentive / Bonus Tracking (Phase 2)**
- Does the Incentives tab on the employee profile have an approval workflow UI (approve/reject pending incentives)?
- Is there a monthly budget cap field — where does it live?
- What table stores incentive records?

**6. Client NPS / Satisfaction (Phase 2)**
- Is there an NPS or satisfaction survey trigger on job completion?
- Is the 30-day throttle logic implemented? Where?
- What table stores survey responses?

**7. HR Policies**
- Do these tables exist: `company_pay_policy`, `company_attendance_policy`, `company_leave_policy`?
- Does the HR Policies tab in Company Settings render an editable form or is it a blank stub?

**8. Support Center**
- Does a Support Center exist? What route?
- Are 10 article categories defined? List them.
- Is there a search function?
- Is role-aware content filtering implemented (Super Admin sees editor, others do not)?
- What table stores articles?

Report findings for each item numbered 1–8. Be specific. File names, table names, and whether each feature is complete, partial, or missing.

---

Paste both prompts sequentially and drop the output back here. Once I have the results I'll produce the full audit summary and we'll sequence everything from there.