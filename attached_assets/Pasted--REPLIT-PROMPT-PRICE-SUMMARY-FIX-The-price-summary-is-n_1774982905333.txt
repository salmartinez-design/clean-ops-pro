**REPLIT PROMPT — PRICE SUMMARY FIX:**

The price summary is not appearing in the booking widget sidebar after Step 2 is completed. By the time the client reaches Step 3 (add-ons), they have already selected their scope and entered square footage — the price should be calculating and displaying in the sidebar immediately.

Fix the following:

**Fix 1 — Price summary must appear starting at Step 2:**

As soon as the client selects a scope on Step 2, call `/api/public/calculate` with the selected scope and square footage and display the result in the sidebar. Do not wait until Step 3 or later. The price should update in real time as the client changes square footage, bedrooms, or scope selection.

**Fix 2 — Price summary must persist through all steps:**

Once calculated, the price summary must remain visible in the sidebar on Steps 2, 3, 4, and 5. It must never disappear between steps.

The price summary tile should show:
- Scope name
- Square footage
- Base price
- Each selected add-on with its price (updates live as add-ons are toggled on Step 3)
- Bundle discount as a negative line if oven + fridge both selected
- If recurring upsell accepted — show "First Visit" price and "Recurring" price separately
- Total in bold

Position the price summary tile in the sidebar above Office Locations on desktop. On mobile show it as a compact sticky bar at the bottom of the screen above the Continue button — scope name and total only, tappable to expand.

**Fix 3 — Real-time updates:**

When the client changes square footage the price recalculates immediately. When they add or remove an add-on on Step 3 the total updates immediately. No page reload needed.

After fixing, confirm by walking through the flow:
1. Select Deep Clean, enter 1500 sqft — price summary shows $434.00 in sidebar
2. Advance to Step 3 — price summary still shows $434.00
3. Select Oven and Refrigerator add-ons — price updates to $514.00 with bundle discount shown
4. Advance to Step 4 — price summary still shows $514.00
5. Advance to Step 5 — price summary still shows $514.00

Report pass or fail for each step.