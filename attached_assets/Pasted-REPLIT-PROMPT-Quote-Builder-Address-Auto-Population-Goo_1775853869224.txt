REPLIT PROMPT — Quote Builder: Address Auto-Population + Google Maps Verification
Fix the service address field in Step 1 of /quotes/new. No other changes.

1. AUTO-POPULATE ON CLIENT LOAD
When an existing client is selected from the search, immediately populate the Service Address and Zip Code fields from the client record (service_address and zip_code columns). This is not happening currently — fix it.
Also populate the Unit/Suite field from unit_suite if that column exists on the client record.

2. GOOGLE MAPS ADDRESS AUTOCOMPLETE
Apply the same Google Maps Places autocomplete implementation used on the booking widget (/book/phes-cleaning) to the Service Address field in the quote builder. Use VITE_GOOGLE_MAPS_API_KEY — both keys are already in Replit Secrets.
Behavior:

As the user types in the Service Address field, show Google Places autocomplete suggestions restricted to componentRestrictions: { country: 'us' } and types: ['address']
On selection of a suggestion, parse the result and auto-fill:

Service Address field: street number + route (e.g. "11535 Eldridge Ave")
Zip Code field: postal_code component
City/State if those fields exist on the form


After a suggestion is selected OR after an existing client's address is loaded, immediately trigger the zone check against the populated zip (same logic already built — reuse it)


3. ADDRESS VERIFICATION INDICATOR
After address is populated (either from client load or manual entry + Places selection), show a verification indicator directly below the address field:
Verified via Google (Places suggestion was selected or address geocodes cleanly):
Background: #EAF3DE
Border: 1px solid #639922
Border-radius: 6px
Padding: 6px 12px
Font-size: 12px
Color: #3B6D11
Text: "Address verified — [Full formatted address from Google]"
Unverified (address was typed manually without selecting a Places suggestion):
Background: #FAEEDA
Border: 1px solid #BA7517
Border-radius: 6px
Padding: 6px 12px
Font-size: 12px
Color: #854F0B
Text: "Address not verified — select from suggestions to confirm"
For client-loaded addresses — run a one-time geocode lookup via the Google Maps Geocoding API using the loaded address string. If the geocode returns a result with geometry.location_type = 'ROOFTOP' or 'RANGE_INTERPOLATED', show the verified state. If no result or low-confidence result, show the unverified amber state.
Store address_verified: boolean in the quote form state and include it in the save payload (ALTER TABLE quotes ADD COLUMN IF NOT EXISTS address_verified BOOLEAN DEFAULT FALSE — run in startup migration).

4. ZONE CHECK TRIGGER
The zone check (green/red banner already built) must fire automatically in two cases:

When a client is loaded and their zip populates the zip field
When a Places suggestion is selected and the zip is parsed from the result

Do not require the user to manually blur the zip field in these two cases — trigger it programmatically after the zip value is set.Sonnet 4.6Extended