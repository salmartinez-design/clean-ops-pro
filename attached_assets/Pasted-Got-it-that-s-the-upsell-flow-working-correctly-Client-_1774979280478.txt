Got it — that's the upsell flow working correctly. Client selected Deep Clean, then opted into recurring weekly service from the upsell card. The summary should show both: Deep Clean as the first job, recurring weekly going forward.

---

**REPLIT PROMPT — SIDEBAR FIXES + MOBILE OPTIMIZATION:**

Make these changes to the booking widget at `/book/phes-cleaning`. Do not touch any pricing logic, form fields, or step navigation.

**Fix 1 — Price breakdown on all steps:**

The price summary disappeared from Step 5. Add it back. It must show on every step from Step 2 onward. Show:

- Selected scope name
- Square footage
- Base price
- Each selected add-on with its price
- Bundle discount as a negative line if applicable
- If recurring upsell was accepted — show both "First Visit (Deep Clean)" price and "Recurring Weekly" price as separate line items
- Total in bold

Position above Office Locations in the sidebar on desktop. On mobile it goes between the form content and the Continue button — not in a sidebar.

**Fix 2 — Merge Business Hours into Contact tile:**

Remove the standalone Business Hours tile. Merge into Contact Information:

```
CONTACT INFORMATION
(773) 706-6000
info@phes.io

Monday – Friday    9:00 AM – 6:00 PM
Saturday           9:00 AM – 12:00 PM
Sunday             Closed
```

**Fix 3 — Sidebar order desktop:**
1. Contact Information (with hours merged)
2. Price Summary (Steps 2–5)
3. Office Locations
4. Before You Book (policies accordion)

**Fix 4 — Rename policies header:**
Change "POLICIES & HOUSE RULES" to "BEFORE YOU BOOK" everywhere in the widget.

**Fix 5 — Full mobile optimization:**

The widget must be fully usable on a phone. Apply these rules:

- No sidebar on mobile — it does not exist. All sidebar content is hidden on screens under 768px
- Contact info, hours, price summary, and office locations are hidden on mobile entirely — client already entered their info
- Price summary on mobile shows as a compact sticky bar at the bottom of the screen above the Continue/Book It button — shows scope name and total price only, tappable to expand full breakdown
- All form fields must be full width on mobile — no side by side columns
- Step progress bar at the top must be visible and scrollable on small screens — show step number and name for current step only on very small screens
- Buttons must be full width on mobile — Back and Book It side by side at minimum 48px touch target height
- Add-on cards must stack vertically on mobile, not in a grid
- Scope cards must stack vertically on mobile
- Cadence pills must be full width and tappable on mobile
- The Very Dirty callback form must be full width on mobile
- Date picker must be full width on mobile
- Card input fields (Stripe Elements) must be full width on mobile
- No horizontal scrolling anywhere at any screen size
- Test at 375px width (iPhone SE) and 390px width (iPhone 14) before closing

After all changes, confirm by screenshotting or describing the mobile layout at Step 1, Step 2, and Step 5. Report any elements that are still overflowing or not touch-friendly.