Two issues — one critical:

**Issue 1 — CRITICAL: Deep Clean price is wrong**

The estimate summary shows:
- 1,500 sqft Deep Clean
- Est. Hours: 6.2h
- Base Price: **$347.20**

But your confirmed pricing audit shows:
- 1,500 sqft Deep Clean = **$434.00** (16/16 audit test passed)

The math: 6.2 hours × $70/hr = **$434.00** — that's correct hourly math. But the widget is showing $347.20. That's $56/hr not $70/hr. Something is overriding the Deep Clean hourly rate in the widget display.

**Issue 2 — Frequency mismatch**

Estimate summary says "Frequency: weekly" but the upsell card has "Every 2 Weeks" selected. The summary isn't updating when cadence changes.

**Issue 3 — Upsell recurring rate needs verification**

Showing $224.25 biweekly for 1,500 sqft. Let me check:
- New formula: tier_hours × $65/hr biweekly
- $224.25 ÷ $65 = 3.45 hours for 1,500 sqft recurring

That seems low — recurring should take similar time to Deep Clean at 6.2h. Need to verify what hours the recurring tier is using for 1,500 sqft.

---

**REPLIT PROMPT — PRICING DISPLAY FIXES:**

Three bugs to fix in the booking widget price calculations:

**Fix 1 — Deep Clean base price incorrect:**

The Deep Clean price is showing $347.20 for 1,500 sqft but should be $434.00. The widget is calculating at approximately $56/hr instead of $70/hr.

Find where the Deep Clean hourly rate is being applied in the widget price calculation. The correct rate is **$70/hr** for Deep Clean. Do not use any other rate, multiplier, or override for Deep Clean pricing. 

Verify: 1,500 sqft Deep Clean → must return **$434.00** exactly. This matches the confirmed 16/16 pricing audit result.

**Fix 2 — Estimate summary frequency not updating:**

The Estimate Summary panel on the right side shows "Frequency: weekly" even when "Every 2 Weeks" is selected in the upsell card. Fix the summary panel to update in real time when:
- A cadence pill is selected in the upsell card
- The customer changes their cadence selection

The summary must always reflect the currently selected cadence.

**Fix 3 — Verify recurring tier hours:**

The recurring rate calculation derives hours from the pricing tier. For 1,500 sqft, confirm the tier hours being used are consistent with actual Phes cleaning times. The recurring tier base price at $65/hr should use the same sqft-to-hours mapping as Deep Clean.

Run these verification checks and log results:
```
1,500 sqft Deep Clean:    should be $434.00 at $70/hr
1,500 sqft Biweekly:      log actual result
1,500 sqft Weekly:        log actual result
1,500 sqft Every 4 Wks:  log actual result
```

Report all four values before republishing so we can verify against real Phes invoices.

Do not touch the pricing engine tables. Fix the widget calculation layer only.