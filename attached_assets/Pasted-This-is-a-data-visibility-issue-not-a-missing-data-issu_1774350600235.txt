This is a data visibility issue, not a missing data issue. The records are in the database — we confirmed that. Something in the query layer is filtering them out. Paste this into Replit:

INVESTIGATE: Employees and Customers Not Showing for PHES Owner Login
salmartinez@phes.io is logging in correctly and returning company_id=1. But the mobile and desktop views are only showing a subset of customers and none of the real PHES employees — only seeded demo names.
Run the following diagnostics and fix any issues found:
1. Employees query
Check what the /api/employees endpoint returns when called with the PHES JWT (company_id=1). Compare against:
sqlSELECT id, name, role, status, branch_id FROM employees WHERE company_id = 1;
Expected: 27 records including real PHES technicians. If the API returns fewer, identify what WHERE clause or RLS filter is reducing the result — check for status = 'active' filters, branch_id mismatches, or RLS policies requiring a specific user UUID rather than company_id.
2. Customers query
Check what /api/customers returns for company_id=1. Compare against:
sqlSELECT COUNT(*) FROM customers WHERE company_id = 1;
Expected: 1,231 total. If the API returns only 10, check for hardcoded LIMIT clauses, pagination defaults cutting off results, or a filter that accidentally scopes to a branch_id instead of the full company.
3. Branch filter check
The mobile UI shows an "Oak Lawn" branch pill. Confirm whether the customers and employees endpoints are filtering by branch_id when no branch should be required to see all records. The owner role should see all branches by default.
4. RLS policy check
Run:
sqlSELECT * FROM pg_policies WHERE tablename IN ('employees', 'customers');
Confirm the RLS policies allow SELECT where company_id = auth.jwt() ->> 'company_id' — not where user_id = auth.uid(). If policies are filtering by auth.uid() the migrated records will be invisible because they were inserted without a matching Supabase auth UUID.
Fix whatever is causing the mismatch and confirm with row counts returned by the API matching the database counts.

My bet is on that last point — RLS filtering by auth.uid() instead of company_id. The migrated records were bulk-inserted and never had a Supabase auth user attached to them, so any policy requiring a matching UID silently returns zero rows.