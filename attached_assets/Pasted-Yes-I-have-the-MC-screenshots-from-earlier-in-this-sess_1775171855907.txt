Yes, I have the MC screenshots from earlier in this session. Here's what MC does that Qleno isn't:

**MC's logic:** The *person* and the *property* are separate objects. Client data lives with the client. House data lives with the house. Job data lives with the job. Everything is grouped by what it *belongs to*, not just dumped in sequence.

Qleno is currently mixing alarm codes (house) with billing (client) with job history (job) all in one flat list. That's why it feels wrong.

---

**PASTE INTO REPLIT AGENT:**

```
Redesign the customer profile page (/customers/:id) with proper information 
architecture. Group data by what it belongs to: CLIENT data with CLIENT data, 
HOUSE/PROPERTY data with HOUSE data, JOB data with JOB data. 
This mirrors how MaidCentral organizes client profiles.

Keep all existing features and all existing API calls. 
Layout and organization changes only unless a field needs to be wired.

═══════════════════════════════════════════════
OVERALL STRUCTURE
═══════════════════════════════════════════════

STICKY HERO STRIP (unchanged, full width, ~64px):
Back button | Avatar initials | Client name | ACTIVE / RECURRING / WEEKLY 
badges | CL-XXXX | Next: [date] | LTV dark box | Schedule Job | Message | 
Invoice | Edit buttons

BELOW HERO: Two-panel layout
Left panel: 260px fixed, sticky, does not scroll with main content
Right panel: flex: 1, fills all remaining width, NO max-width cap

═══════════════════════════════════════════════
LEFT PANEL — CLIENT STATS (always visible)
═══════════════════════════════════════════════
Card titled "Client Stats":
  Client Since
  Last Cleaning
  Next Cleaning  ← highlight in brand color if within 7 days
  Lifetime Revenue
  Last 12 Months
  Avg Bill (12mo)
  Total Visits
  Pending Jobs
  Skips
  Bumps
  eCard Rate
  Tech Consistency (e.g. "3 techs across 47 visits")

No calendar in left panel. No other cards needed here.

═══════════════════════════════════════════════
RIGHT PANEL — 4 TABS
═══════════════════════════════════════════════
Tabs: CLIENT | PROPERTY | JOBS | ADMIN
Active tab uses var(--brand) underline.
Tab bar is sticky so it stays visible as user scrolls tab content.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAB 1: CLIENT
Who this person is. How to reach them. How they pay.
Desktop: 2-column CSS grid (grid-template-columns: 1fr 1fr)
Mobile: single column
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Left column:
  CONTACT & BASIC INFO card
    - First Name, Last Name (display only, Edit opens slide-in)
    - Phone
    - Email
    - Client Since
    - Acquisition Source

  BILLING & PAYMENTS card
    - Payment Method (card on file or "None on file")
    - Send Card Link via Email button
    - Send Card Link via SMS button
    - Total Paid (All Time)
    - Last Payment date + amount
    - Payment Method on File label

Right column:
  INVOICES card
    - Table: Date | Invoice # | Amount | Balance | Status
    - Paginated (10 per page)
    - "No invoices yet" empty state

  QUICKBOOKS card
    - Connected/Not Connected status
    - If not connected: Link to QBO Customer + Create in QBO buttons
    - If connected: show Outstanding Balance + Overdue Balance
    - Last Invoice summary (invoice #, date, amount, amount due)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAB 2: PROPERTY
Everything about where we go and how we get in.
Desktop: 2-column CSS grid
Mobile: single column
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Left column:
  SERVICE ADDRESSES card
    - List all addresses on file (street, city, state, zip)
    - Each address shows its service zone if assigned
    - + Add Another Home button

  ACCESS & ENTRY card  ← CRITICAL for technicians
    - Entry Instructions (textarea)
    - Alarm / Lockbox Code (text input, displayed prominently)
    - Pets / Equipment Notes (textarea)
    - Wire to existing DB columns (entry_instructions, 
      lockbox_code, pets_notes or equivalent)
    - Edit button saves inline

  CLIENT NOTES card (full width of left column, auto-saves on blur)

Right column:
  RECURRING SCHEDULE card
    - Frequency | Start Date | Scope (3 columns)
    - Notes
    - Edit Service Details button (opens existing edit flow)

  RATE HISTORY card
    - Table of rate changes with dates
    - "No rate changes recorded" empty state

  RATE LOCKS card
    - Current active rate lock if any (rate, expiry, conditions)
    - + Add Rate Lock button
    - Edit button

  HOME IMAGES card
    - Photo grid (thumbnails)
    - Upload Photo button
    - "No job photos on record. Photos are added by technicians 
      from the mobile app during service visits." empty state

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAB 3: JOBS
All historical and logged activity for this client.
Full width (single column — tables need space)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  JOB HISTORY section
    - Header: "X visits | $X total"
    - Table: Date | Tech(s) | Scope | Duration | Amount | Status
    - Paginated (10 per page, Prev / Next)
    - "No job history records found" empty state

  SCORECARDS section
    - Table: Job Date | Score | Technician | Comments | Actions
    - "No scorecards yet" empty state

  INSPECTIONS section
    - Create New Inspection button
    - Table: Date | Inspector | Score | Result | Notes
    - "No inspections on record" empty state

  COMMUNICATION LOG section
    - Log Communication form: Direction dropdown + Channel dropdown + 
      Summary textarea + Log Entry button
    - List of past log entries below form
    - "No communication logs yet" empty state

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAB 4: ADMIN
Everything operational but not part of daily dispatch workflow.
Desktop: 2-column CSS grid for the top cards, 
full-width collapsible sections below.
Mobile: single column
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Left column (top):
  CLIENT PORTAL card
    - Status: "No Portal Access" or active
    - Send Portal Invitation button
    - View Portal link

  CONTACTS & NOTIFICATIONS card
    - + Create Notification button
    - List of notification contacts
    - "No notification contacts configured" empty state

Right column (top):
  TECHNICIAN PREFERENCES card
    - Warning banner: "Do Not Schedule preferences are enforced 
      on the dispatch board."
    - + Create Preference button
    - Table: Technician | Preference | Notes | Actions
    - "No preferences set" empty state

  CONTACT TICKETS card
    - + Create New Ticket button
    - Table: Created | Type | Job ID | Notes | Created By
    - "No tickets yet" empty state

Full-width collapsible sections below the 2-column top:
  QUOTES (collapsible, expanded by default)
    - + New Quote button
    - Quote list or "No quotes yet" empty state

  AGREEMENTS (collapsible, expanded by default)
    - Send Agreement button
    - Table: Agreement | Sent | Status | Signed
    - "No agreements sent yet" empty state

  ATTACHMENTS (collapsible, collapsed by default)
    - Upload File button
    - Category filter tabs: All | Home Photo | Document | 
      Inspection | Agreement | Other
    - File grid or "No files" empty state

═══════════════════════════════════════════════
RESPONSIVE BEHAVIOR
═══════════════════════════════════════════════
Desktop (>= 768px):
  - Left 260px stats panel visible
  - Right panel 2-column grid where specified
  - Tab bar sticky below hero strip

Mobile (< 768px):
  - Left panel hidden; replace with a compact 2-stat summary 
    row below hero: "Next Job: Apr 5 · LTV: $17,495"
  - All tab content single column
  - Tab bar scrollable horizontally if needed

═══════════════════════════════════════════════
CONSTRAINTS — DO NOT CHANGE
═══════════════════════════════════════════════
- Hero strip: unchanged
- All existing API endpoints: unchanged  
- All existing DB columns: unchanged
- All existing edit flows (slide-in panels, modals): unchanged
- Design system: #F7F6F3 background, #FFFFFF cards, 
  #1A1917 text, #E5E2DC borders, Plus Jakarta Sans only, 
  no emojis, no dark mode
- The left stats panel position: sticky, 
  top: 64px, height: calc(100vh - 64px), overflow-y: auto
```