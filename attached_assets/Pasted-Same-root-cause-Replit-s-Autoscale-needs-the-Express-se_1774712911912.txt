Same root cause — Replit's Autoscale needs the Express server as the run command, not static file serving. The `_redirects` approach doesn't work here. The fix needs to go in the Express server itself.

**REPLIT PROMPT — SPA CATCH-ALL ROUTE FIX:**

The deployment keeps failing because Replit Autoscale requires a Node.js server process with a run command. The `_redirects` static file approach does not work on this hosting type.

Fix the following:

**Step 1 — Add catch-all route to Express server:**

In `server/index.ts`, after all API routes are registered and after the static file middleware, add a catch-all route that serves `index.html` for all non-API routes:

```javascript
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// Serve static files from the React build
app.use(express.static(path.join(__dirname, '../../cleanops-pro/dist/public')));

// Catch-all: serve index.html for all non-API routes (SPA fallback)
app.get('*', (req, res) => {
  if (!req.path.startsWith('/api')) {
    res.sendFile(path.join(__dirname, '../../cleanops-pro/dist/public/index.html'));
  }
});
```

Adjust the path to `dist/public` to match the actual build output directory for the React frontend. Check where `index.html` is produced after `npm run build` and use that exact path.

**Step 2 — Confirm run command in .replit:**

The `.replit` deployment section must be:
```
[deployment]
run = ["node", "artifacts/api-server/dist/index.mjs"]
build = ["sh", "-c", "pnpm --filter @workspace/cleanops-pro run build"]
```

This matches the confirmed working run command from the previous successful deploy (`ca77305e` — published about 15 hours ago).

**Step 3 — Confirm PORT:**

In `server/index.ts`, confirm:
```javascript
const PORT = process.env.PORT || 3000;
app.listen(PORT, '0.0.0.0', () => {
  console.log(`Server running on port ${PORT}`);
});
```

The `'0.0.0.0'` binding is required for Cloud Run — it must listen on all interfaces not just localhost.

**Step 4 — Do not touch the database or any frontend code in this fix.**

Redeploy after changes. All 5 pipeline stages must go green before testing the booking widget URL.