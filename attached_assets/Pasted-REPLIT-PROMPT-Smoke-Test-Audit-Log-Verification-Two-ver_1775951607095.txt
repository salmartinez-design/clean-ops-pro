REPLIT PROMPT — Smoke Test + Audit Log Verification
Two verification checks only. No new features, no schema changes, no page modifications.

CHECK 1 — SMOKE TEST: force run and surface results
In the Super Admin panel at /admin, the “Run now” button should trigger POST /api/admin/smoke-tests/run. Verify this endpoint:
	1.	Actually calls runSmokeTests() — add a console log at the start of the function: [SMOKE] Manual run triggered at [timestamp]
	2.	Returns the full results array, not just {passed, failed, total} — the UI needs the individual test names and error messages to display correctly
	3.	The response shape must be:

{
  "passed": 11,
  "failed": 1,
  "total": 12,
  "duration_ms": 847,
  "results": [
    { "name": "DB connection", "status": "pass", "ms": 12 },
    { "name": "Zone lookup — 60464 returns Tinley zone with correct color", "status": "fail", "error": "Wrong color: null", "ms": 8 }
  ]
}


If the endpoint currently returns only {passed, failed, total}, update it to include the full results array.
Add explicit console logging for the two zone tests:
In the zone lookup smoke tests, before the assert, add:

console.log(`[SMOKE ZONE] zip 60453 result:`, r.rows);
console.log(`[SMOKE ZONE] zip 60464 result:`, r.rows);


This surfaces exactly what’s in the DB for those zips so we can diagnose the color issue without guessing.

CHECK 2 — AUDIT LOG: write a test entry and verify it lands
Add a POST /api/admin/audit-test endpoint (Super Admin JWT required) that:
	1.	Writes a test audit log entry:

await logAudit({
  companyId: PHES_COMPANY_ID,
  userId: req.user.id,
  userEmail: req.user.email,
  userRole: req.user.role,
  action: 'SMOKE_TEST',
  entityType: 'system',
  entityId: null,
  oldValues: null,
  newValues: { test: true, timestamp: new Date().toISOString() },
  req
});


	2.	Then immediately reads it back:

const verify = await db.query(
  `SELECT * FROM audit_logs 
   WHERE action = 'SMOKE_TEST' 
   ORDER BY created_at DESC LIMIT 1`
);


	3.	Returns the result:

{
  "written": true,
  "row": {
    "id": "...",
    "company_id": "...",
    "user_email": "sal@cleanopspro.com",
    "action": "SMOKE_TEST",
    "created_at": "..."
  },
  "audit_logging_healthy": true
}


If the row is not found after writing: return { "audit_logging_healthy": false, "error": "Row not found after insert" }.
Add a “Test audit logging” button to the Super Admin health check widget on /admin:
	•	Calls POST /api/admin/audit-test
	•	Shows green confirmation: “Audit log write confirmed — [timestamp]” if healthy
	•	Shows red error if not healthy
	•	Button style: outline, 12px, same row as the “Run now” smoke test button

ALSO: surface the zone color console output in the UI
In the smoke test results widget on /admin, when the zone lookup tests fail, show the raw error message inline below the test name in 11px #A32D2D:

Zone lookup — 60464 returns Tinley zone with correct color
→ Wrong color: null


This means you can diagnose the zone issue directly from the Super Admin UI without opening the Replit console.​​​​​​​​​​​​​​​​