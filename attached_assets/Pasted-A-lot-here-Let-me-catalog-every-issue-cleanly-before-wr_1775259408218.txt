A lot here. Let me catalog every issue cleanly before writing the prompt:
	1.	Hero strip mobile — zone not visible, 2026 revenue not visible
	2.	Invoice tile — left border trim only, Balance column cuts off, needs horizontal scroll
	3.	Property tab — zone color not showing on address card
	4.	Recurring schedule — frequency listed twice, scope listed twice, tech not shown, duration in minutes not hours, redundant section headers
	5.	Calendar off screen — not contained to mobile viewport
	6.	April 3 phantom job — Jim is weekly Thursday, April 3 is a Friday. That job shouldn’t exist. Also the reschedule modal shows wrong data — Alejandra Cuervo, $120, 9:00AM — that’s not Jim’s data at all, it’s a ghost job from seed data
	7.	Reschedule API error — {"error":"Failed to reschedule job"} — backend endpoint broken

Make these targeted mobile and data fixes. Do not touch desktop layout or any other page.
1. Delete the phantom April 3 job for Jim Schultz:
In the jobs table, delete any job for Jim Schultz (client_id = CL-0023, PHES tenant) where scheduled_date = '2026-04-03'. This is not a valid Thursday job. Jim is weekly Thursdays only. Verify no other non-Thursday jobs exist for Jim after April 3, 2026.
2. Fix the reschedule API endpoint:
The /api/jobs/:id/reschedule endpoint is returning {"error":"Failed to reschedule job"}. Debug and fix the error — likely a missing field validation, wrong column name, or transaction rollback. The endpoint must:
	∙	Accept: new_date, reason, notes, changed_by
	∙	Update scheduled_date on the job record
	∙	Insert into job audit log
	∙	Return success with updated job data
3. Mobile hero strip — add zone and 2026 revenue:
On mobile (screen width < 768px):
	∙	Below the client name, show a small colored zone dot + zone name in one line (same color as zone system)
	∙	The LTV tile on mobile must show both: Lifetime value AND 2026 revenue — stack them vertically if needed, both must be readable without scrolling
4. Mobile invoice tile — fix border and add scroll:
	∙	Left accent border must match right border (symmetric, or remove accent border entirely on mobile)
	∙	Invoice table must be horizontally scrollable on mobile — wrap the table in a overflow-x: auto container so Balance and Status columns are reachable by swiping
	∙	Column headers must not clip
5. Property tab — add zone color to address card:
Each service address card must show a colored zone dot + zone name label beneath the address. Use the existing zone color mapping. This applies on both mobile and desktop.
6. Recurring schedule — clean up redundancy:
The recurring schedule section currently shows frequency twice, scope twice, and duration in minutes. Fix:
	∙	Show frequency ONCE: “Weekly — Thursdays”
	∙	Show scope ONCE: “Hourly Standard Cleaning”
	∙	Duration: show as hours not minutes (“4 hrs” not “240 min”)
	∙	Add technician: show assigned tech name(s) — pull from the recurring_schedule_technicians junction table or the technician_id field
	∙	Remove the duplicate “RECURRING SCHEDULE” inner card — consolidate into one clean section
	∙	Remove the raw notes field from the recurring schedule display (data is now in proper fields)
7. Calendar mobile containment:
The job calendar must not overflow the mobile viewport. Wrap the entire calendar in overflow-x: hidden on the parent container. On mobile, the 3-month view should stack vertically (one month per row) instead of rendering side by side. Each month must fit within the screen width.
Verify at https://clean-ops-pro.replit.app/customers/23 on mobile:
	∙	No April 3 job on calendar
	∙	All remaining jobs land on Thursdays only
	∙	Zone dot visible in hero strip
	∙	Calendar fits within screen width
	∙	Recurring schedule shows tech name, hours not minutes, no duplicates
	∙	Reschedule modal saves without error
Do not push to GitHub until confirmed.​​​​​​​​​​​​​​​​