Yeah — I can see exactly what happened. Replit's agent defaulted to its own design patterns instead of following our spec. The typography is wrong, the cards are generic, the color system isn't applied, and the tenant branding layer is completely missing. This is a common Replit agent problem — it drifts from the brief on UI. Here's what needs to happen:

**What I'm seeing in the screenshots:**

1. Font is generic monospace throughout — should be Playfair Display for headings, DM Mono for data
2. The job cards look like default Tailwind card components — no personality
3. Sidebar is plain dark gray — should have teal accent system
4. No tenant color injection — the whole platform should swap colors based on company brand
5. Company and Loyalty pages aren't wired up at all
6. The PHES logo (the winged P mark) isn't being pulled into the header correctly
7. Metric cards are functional but visually flat — need the proper treatment from our mockup

Here's the overhaul prompt to paste into Replit right now:

---

```
STOP all current work. We need a complete UI overhaul before continuing 
any feature development. The current UI does not match our design spec. 
Here is exactly what needs to be rebuilt.

═══════════════════════════════════════════════════════
PART 1 — TYPOGRAPHY (Fix this first, everything depends on it)
═══════════════════════════════════════════════════════

Install and import these Google Fonts in index.html or the root layout:
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=DM+Mono:wght@300;400&display=swap" rel="stylesheet">

Apply these rules globally in your CSS:

  /* Page headings, dashboard title, section titles */
  .heading-display {
    font-family: 'Playfair Display', serif;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  /* ALL body text, labels, nav items, table data, badges, stats */
  body, * {
    font-family: 'DM Mono', monospace;
    font-weight: 300;
  }

  /* Numbers and metrics specifically */
  .metric-value {
    font-family: 'Playfair Display', serif;
    font-weight: 900;
  }

FORBIDDEN: Do not use Inter, Roboto, Arial, or any system font anywhere.

═══════════════════════════════════════════════════════
PART 2 — TENANT BRANDING SYSTEM (Most important feature)
═══════════════════════════════════════════════════════

Every company in the system has two brand values stored in the companies table:
  - brand_color (hex, e.g. "#0065A5") 
  - logo_url (uploaded file URL from Cloudflare R2)

These must be added to the companies table if not already there:
  ALTER TABLE companies ADD COLUMN IF NOT EXISTS brand_color VARCHAR(7) DEFAULT '#00C9A7';
  ALTER TABLE companies ADD COLUMN IF NOT EXISTS logo_url TEXT;

When a user logs in, fetch their company's brand_color and logo_url.
Inject the brand_color as a CSS custom property on the root element:
  document.documentElement.style.setProperty('--tenant-color', company.brand_color);

Then replace ALL hardcoded teal/green accent references with var(--tenant-color).

This means:
  - Sidebar active item highlight = var(--tenant-color)
  - "New Job" / "Add Client" CTA buttons = var(--tenant-color)
  - Badge accents = var(--tenant-color)
  - Revenue trend chart line = var(--tenant-color)
  - Progress bars = var(--tenant-color)
  - Loyalty points badges = var(--tenant-color)
  - Border accents on cards = var(--tenant-color)

The company LOGO must appear in the top-left sidebar header, replacing 
the "CleanOps Pro" wordmark. If no logo is uploaded, show the app name.
Logo dimensions: max-height 36px, object-fit contain.

═══════════════════════════════════════════════════════
PART 3 — GLOBAL LAYOUT + DARK MODE SPEC
═══════════════════════════════════════════════════════

Root background:     #0D0D0D
Sidebar background:  #111111
Card background:     #161616
Card border:         1px solid #252525
Hover card border:   1px solid var(--tenant-color)
Header background:   #111111 with border-bottom 1px solid #252525

Text colors:
  Primary:    #E8E0D0
  Secondary:  #888780
  Tertiary:   #555550
  Accent:     var(--tenant-color)

Metric cards:
  background: #1A1A1A
  no border
  border-radius: 8px
  padding: 14px
  label: 11px DM Mono #888780 uppercase letter-spacing 0.08em
  value: 28px Playfair Display 900 #E8E0D0

═══════════════════════════════════════════════════════
PART 4 — SIDEBAR REDESIGN
═══════════════════════════════════════════════════════

Sidebar width: 220px
Background: #111111
Top section: company logo (from logo_url) OR app name + company name below it
             company name: 12px DM Mono #888780

Navigation sections:
  OPERATIONS (label: 10px DM Mono #555550 uppercase letter-spacing 0.1em)
    - Dashboard
    - Jobs  
    - Employees
    - Customers
    - Invoices
    - Payroll

  TOOLS
    - Cleancyclopedia (NEW — link to /cleancyclopedia)

  CONFIGURATION
    - Loyalty  (wire this up — it was broken)
    - Company  (wire this up — it was broken)

Active nav item:
  background: rgba(var(--tenant-color-rgb), 0.12)
  border-left: 3px solid var(--tenant-color)
  border-radius: 0
  color: var(--tenant-color)
  
Note: also store var(--tenant-color-rgb) as the RGB values of brand_color
so rgba() works. Parse the hex to RGB when setting the CSS variable.

Inactive nav item:
  color: #888780
  hover: color #E8E0D0, background #1A1A1A

Bottom of sidebar:
  User name (DM Mono 12px #E8E0D0)
  Role badge (10px, tenant color background at 15% opacity)
  Sign Out button

═══════════════════════════════════════════════════════
PART 5 — DASHBOARD OVERHAUL
═══════════════════════════════════════════════════════

Page title: "Dashboard" in Playfair Display 42px #E8E0D0 font-weight 700
Subtitle: "Overview of operations for the current week." DM Mono 13px #888780

Top metric row (4 cards):
  - Total Revenue (with trend %)
  - Jobs Completed (with "X in progress" sub)
  - Active Employees (with avg score sub)
  - Flagged Clock-Ins (with "Requires review" sub)

  Each card: background #1A1A1A, no border, radius 8px
  Icon: small SVG in top-right, color var(--tenant-color) at 60% opacity
  
Revenue Trend chart:
  Chart line color: var(--tenant-color)
  Chart fill: var(--tenant-color) at 15% opacity
  Grid lines: #252525
  Label text: DM Mono 11px #555550

Recent Jobs table on dashboard:
  Header row: DM Mono 11px #555550 uppercase
  Data rows: DM Mono 13px #E8E0D0
  Status badges: COMPLETE = #EAF3DE text #27500A, SCHEDULED = #E6F1FB text #0C447C
  Alternating row hover: #1A1A1A

Top Employees panel:
  Rank number: DM Mono 11px var(--tenant-color)
  Employee name: DM Mono 13px #E8E0D0 font-weight 400
  Avatar initials circle: background var(--tenant-color) at 20% opacity, text var(--tenant-color)

═══════════════════════════════════════════════════════
PART 6 — JOB CARDS OVERHAUL (Jobs page)
═══════════════════════════════════════════════════════

The current job cards look like generic Tailwind components. Rebuild them:

Each job card:
  background: #161616
  border: 1px solid #252525
  border-left: 3px solid [frequency color] (see below)
  border-bottom: 3px solid [status color] (see below)
  border-radius: 10px
  padding: 16px

Frequency left-border colors (match MaidCentral system):
  Every week:      #00C9A7 (or var(--tenant-color))
  Every 2 weeks:   #378ADD
  Every 3 weeks:   #EF9F27
  Monthly:         #AB47BC
  On demand:       #888780
  Other recurring: #E24B4A

Status bottom-border colors:
  Scheduled:    #252525 (neutral)
  In Progress:  #EF9F27
  Complete:     #3B6D11

Card header row:
  Status badge (left) + Price (right, Playfair Display 20px #E8E0D0)

Client name: Playfair Display 18px #E8E0D0 font-weight 700
Service type: DM Mono 12px var(--tenant-color) uppercase letter-spacing 0.08em

Time + address row: DM Mono 12px #888780

Bottom row:
  Employee avatar (initials, 28px circle, var(--tenant-color) bg at 20%)
  Employee name: DM Mono 12px #888780
  Photo count: "0B / 0A" DM Mono 11px #555550

Filter tabs (All Jobs / Scheduled / In Progress / Completed):
  Active tab: background var(--tenant-color), text #0D0D0D, font-weight 400
  Inactive: border 1px solid #252525, text #888780
  
"+ New Job" button: background var(--tenant-color), text #0D0D0D, DM Mono 13px

═══════════════════════════════════════════════════════
PART 7 — EMPLOYEES PAGE OVERHAUL
═══════════════════════════════════════════════════════

Change "Team Directory" heading to "Employees"
Table header: DM Mono 11px #555550 uppercase letter-spacing 0.08em

Role badges:
  OWNER:      background #EEEDFE, text #3C3489
  ADMIN:      background #E6F1FB, text #0C447C  
  TECHNICIAN: background #EAF3DE, text #27500A

Productivity column: 
  Show as a mini circle progress ring (SVG) with % number inside
  Ring color: var(--tenant-color)
  Track color: #252525

Score column:
  Star icon in var(--tenant-color) + number

Avatar circles: var(--tenant-color) at 20% opacity background

═══════════════════════════════════════════════════════
PART 8 — CUSTOMERS PAGE OVERHAUL
═══════════════════════════════════════════════════════

Loyalty points badges (right column):
  Large pill badge: background var(--tenant-color) at 15% opacity
  Points number: DM Mono 18px font-weight 400 var(--tenant-color)
  "REWARD READY" sub-label: DM Mono 9px var(--tenant-color) uppercase

"Batch Actions" button: border 1px solid #252525, DM Mono 12px #888780

Checkbox circles: border 1px solid #252525, checked = var(--tenant-color) fill

═══════════════════════════════════════════════════════
PART 9 — INVOICES PAGE OVERHAUL
═══════════════════════════════════════════════════════

Metric cards at top (Outstanding / Overdue / Paid / Revenue):
  "TOTAL REVENUE (YTD)" card: border 1px solid var(--tenant-color)
  
Status badges:
  PAID:     background #EAF3DE, text #27500A
  OVERDUE:  background #FAEEDA, text #633806
  DRAFT:    background #F1EFE8, text #444441
  SENT:     background #E6F1FB, text #0C447C

"+ Create Invoice" button: background var(--tenant-color), text #0D0D0D

═══════════════════════════════════════════════════════
PART 10 — COMPANY SETTINGS PAGE (was broken — build it)
═══════════════════════════════════════════════════════

Route: /company
Tabs: General | Branding | Integrations | Payroll Options

BRANDING TAB (most important):

  Brand Color section:
    Label: "Brand Color" DM Mono 13px
    Color picker input (type="color") + hex text input
    Preview section showing how the color will look applied to sidebar nav
    Save button

  Logo section:
    "Logo (dark backgrounds)" — file upload, preview on dark (#161616) background
    "Logo (light backgrounds)" — file upload, preview on light (#F9F7F4) background
    Supported: PNG with transparency, max 2MB
    Both logos stored to Cloudflare R2 at company_id/logos/

  When brand_color or logo is saved:
    UPDATE companies SET brand_color = $1, logo_url = $2 WHERE id = $3
    Immediately apply the new color to the current session (update CSS var)
    Show success toast: "Brand updated — changes are live"

  Template fields section:
    Company Full Name, Phone, Email, Website URL, Social profiles
    External Quoting Tool URL (iFrame embed URL)

GENERAL TAB:
  Company name, address, timezone, pay cadence (weekly/biweekly/semi-monthly)
  Service types active (checkboxes for each job type)

═══════════════════════════════════════════════════════
PART 11 — LOYALTY PAGE (was broken — build it)
═══════════════════════════════════════════════════════

Route: /loyalty

Section 1: Program Style selector (3 cards)
  Points-based | Punch Card | Tiered VIP
  Selected card: border 2px solid var(--tenant-color), background at 8% opacity

Section 2: Earn Rules (toggle list)
  Each rule: toggle switch (on = var(--tenant-color)) + label + points badge
  Rules: Per completed cleaning (50pts) / Per dollar spent (slider 1-10pts/$1) /
         Referral (200pts) / Google review (100pts) / Auto-pay (25pts) / Birthday (50pts)

Section 3: Rewards (toggle list)
  $5 off (250pts) / $10 off (500pts) / Free add-on (400pts) / Free cleaning (1200pts)
  
Points per dollar slider:
  <input type="range" min="1" max="10" value="5">
  Shows: "5 pts per $1 spent" updating live

Save button: background var(--tenant-color)

═══════════════════════════════════════════════════════
PART 12 — GLOBAL POLISH RULES
═══════════════════════════════════════════════════════

1. ALL page titles use Playfair Display 700. No exceptions.
2. ALL data, labels, nav, body copy use DM Mono 300/400. No exceptions.
3. ALL primary CTA buttons use var(--tenant-color) background with #0D0D0D text.
4. NO gradients anywhere.
5. NO box-shadows anywhere (except 0 0 0 Npx focus rings on inputs).
6. ALL status badges use the semantic color system defined above.
7. ALL avatar circles use var(--tenant-color) at 20% opacity for bg.
8. Scrollbars: thin, #252525 track, #444 thumb.
9. Focus rings on all inputs: 0 0 0 2px var(--tenant-color) at 40% opacity.
10. Toast notifications: background #161616, border 1px solid var(--tenant-color),
    text #E8E0D0, DM Mono 13px, appears bottom-right, auto-dismiss 3s.

Do all 12 parts in order. Show me the dashboard first when complete,
then I will verify before we move to the next page.
```

---

A few things worth flagging while this runs:

The tenant color system in Part 2 is the most important piece — once that's wired correctly, every company on your platform gets their own branded experience just from uploading a logo and picking a hex color. That's the MaidCentral "Logos, Marketing & UI" tab you showed in screenshot 8, but done properly so it actually cascades through the entire UI automatically.

The PHES logo in screenshot 9 is strong — the winged P mark in red and blue. Once the logo upload is working in Company > Branding, that mark should appear in your sidebar header replacing "CleanOps Pro." That alone will make the platform feel custom rather than templated.

Once Replit finishes the overhaul, screenshot the dashboard and send it here — we'll review and tighten anything that still looks off before moving forward.